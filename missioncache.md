# SandHybrid Mission Cache

This is the **single canonical product backlog** for SandHybrid. It contains active work, priority, acceptance criteria, permanent invariants, and concise accepted foundations. Release history lives only in `CHANGELOG.md`.

Before changing code:

1. Read this file.
2. Preserve every `OPEN`, `PARTIAL`, `REGRESSION`, or `DEFERRED` mission.
3. Update status and evidence in the same source commit.
4. Never mark visual/runtime behavior complete from compilation or static audits alone.
5. Verify Windows and Linux Release builds before publishing.
6. Move a mission to the archive only after acceptance passes. Runtime contradiction reopens the same ID.
7. During each broad pass, attempt every active mission once. Anything unfinished or runtime-unverified remains active with evidence.
8. This v2.5.9 regression-recovery pass compared early working material behavior with v2.5.8, restored the v2.4.1 Half Water attraction baseline, separated Lava from sparse powder timing, accelerated cyclic vent pressure, rebuilt the Volcano lake basin, preserved liquid/gas volume when Smoke is painted, removed generic Acid-to-Silt conversion, restored the early suspended hive, added workspace tabs, coherent cosmetic material profiles, complete world dimensions, and carried every unfinished mission forward. Packaged visual/runtime behavior remains active until observed; no item is silently omitted or falsely closed.
9. Cache the requested behavior, compatibility risks, affected missions, and acceptance checks before changing source. After implementation, re-read the cache, inspect the complete diff, run deterministic contracts and platform builds, and repeat the impact review until no unaddressed consequence remains; unresolved risk stays active rather than being assumed away.

Statuses: `OPEN`, `PARTIAL`, `REGRESSION`, `DEFERRED`. v2.5.17 is published from `791bf7160ce95ec27c5a31717e845cb6ba95e381` as GitHub's latest normal non-prerelease release after successful main/tag workflows, native Windows/Linux Release builds, 27/27 tests per platform, fresh package audits/checksums, installed-package Vulkan readback on both platforms, and high-DPI paused-cursor observation on Windows. It carries focused evidence for exact macro Water/gas packets, blocked fine fallback, Half Water conservation/motion, canonical hard-coded Fix36 hive contents, and logical cursor ownership. Every other unaccepted mission remains active.

## Priority lanes

- **P0 / primary release gate:** MC-012, MC-013, MC-017, MC-018, MC-026, MC-031, MC-032, MC-038, MC-039, MC-068, MC-074, MC-079, MC-084, MC-097, MC-104, MC-105, MC-106, MC-107, MC-112, MC-113, MC-114, MC-115, MC-116, MC-118, MC-119, MC-120, MC-122, MC-123, MC-124, MC-125, MC-126, MC-127, MC-128, MC-129, MC-130, MC-131, MC-132, MC-133, MC-134, MC-135, MC-136, MC-137, MC-138, MC-139, MC-140, MC-149, MC-150, MC-151, and MC-152. These are the current release blockers and must pass deterministic contracts plus Windows/Linux Release packaging before publication.
- **P1 / runtime correctness:** simulation, conservation, water, atmosphere, ecology, machinery, UI, scene, and performance missions that require packaged observation or deterministic runtime evidence.
- **P2 / architecture and later integration:** streaming, full replacement-runtime cutover, optional subsystem extraction, and EpochEngine integration.
- Priority is a scheduling property, not a second status system. Every unfinished row remains in this table exactly once.

# Active missions

## Production rewrite and sparse world migration

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-101 | PARTIAL | Execute the production rewrite program | `REWRITE_PLAN.md` is the authoritative staged migration plan. Each stage lands behind deterministic contracts, preserves the old runtime until parity passes, updates this ledger in the same commit, and removes compatibility code only after Windows/Linux runtime acceptance. R1 is complete and R2 is initiated. v2.5.5 core evidence: R2 now includes platform-neutral packed Atmosphere, exact packet transactions, actor/medium separation, bounded impulses, atomic machinery/sluicing, and explicit habitat policy; Vulkan integration and cutover remain active. |
| MC-102 | PARTIAL | Sparse 64x64 section metadata | The platform-neutral core owns signed sparse section coordinates, per-section dirty rectangles, four non-touching phases, boundary-halo wakeups, automatic sleep, and clean metadata retirement. Deterministic contracts pass. Runtime dispatch must still prove that clean sections execute zero material work. |
| MC-103 | OPEN | Canonical paged cell storage | Replace duplicated hierarchy authority with one append-only, save-versioned cell store. Hot fields use structure-of-arrays storage; half-water, moisture, atmosphere components, damage, temperature, and material identity survive page load/save exactly. |
| MC-104 | PARTIAL | Transactional liquid and gas packet acceleration | Eligible 8x8 liquid/gas packets are derived from canonical cells, validate into scratch state, and atomically commit or run fine fallback in the same tick. No ownership ping-pong, volume loss, hidden fill, or one-frame classification loop is permitted. v2.5.5 core evidence: `packet_transaction.hpp` provides exact all-or-fallback packet commits and full 8x8 Water packet versus represented fine-unit parity; production scratch validation and same-tick fallback remain. v2.5.13 tranche-one evidence: exact packet transaction contracts and new macro-first/fine-fallback policy assertions revalidate atomic all-or-fallback behavior; production scratch/dispatch observation remains active. |
| MC-105 | PARTIAL | Durable structural solids are metadata only | Stone, iron ore, refined metals, glass, wood, plastics, machines, and every other block-capable material use 8x8 support/cohesion/sleep metadata without whole-tile translation. Once created through `TILES`, surviving cells never auto-release because of occupancy, minority-material, or support heuristics. Only direct per-cell destruction, corrosion, combustion, or a real softened/molten/vapor phase change removes structural state. Runtime fracture and persistence acceptance remain required. v2.5.13 tranche-one evidence: scene-image, terrain, and shader contracts revalidate structural metadata retention and exclusion from whole-tile movement; packaged fracture/persistence remains active. |
| MC-106 | PARTIAL | Canonical packed atmosphere | Atmosphere cells conserve N2/O2/Ar/CO2/H2/He/vapor/contaminants, pressure, and temperature. Painting individual gases modifies composition instead of replacing air, and closed-box tests prove conservation. v2.5.5 core evidence: `PackedAtmosphere` stores N2/O2/Ar/CO2/Ne/H2/He/vapor/contaminants with exact baseline, transfer, enrichment, respiration, pressure, and component conservation; production storage/shaders/saves remain. v2.5.13 tranche-one evidence: packed-atmosphere contracts revalidate exact Earth-air composition, component conservation, pressure-preserving transfer/enrichment, and respiration; production storage/shader/save migration remains active. |
| MC-111 | PARTIAL | Selectable atmosphere component gases | The palette exposes Nitrogen, Oxygen, Argon, Carbon Dioxide, Neon, Hydrogen, and Helium as distinct tools while retaining balanced Atmosphere. Append-only material/save IDs are preserved. In the replacement core, painting a component changes packed local composition and pressure rather than deleting the other atmosphere components; cards show identity, density, and local percentage. v2.5.5 core evidence: The packed API defines all required components and maps current O2/CO2/H2/vapor/contaminant materials without renumbering; N2/Ar/Ne/He palette tools and production painting remain. |
| MC-112 | PARTIAL | Stone, Iron Ore, and Iron cell/tile parity | Save ID 48 is canonically `Iron Ore`; no Iron Shavings identifier or alias remains. Stone, Iron Ore, refined Iron, and every block-capable material use `CELLS` for loose individual pixels and `TILES` for durable structural 8x8 arrangements. Tile metadata never translates the solid wholesale, and surviving tile-built cells never auto-crumble from occupancy or support loss. Static ID, palette, phase, block-capability, and shader contracts pass; packaged placement/fracture proof remains required. v2.5.13 tranche-one evidence: material, terrain, scene-image, and shader validators revalidate Iron Ore ID 48, cell/tile capability, structural retention, and deposit identity; packaged placement/fracture remains active. |
| MC-118 | PARTIAL | Deterministic natural metal and ore deposits | Shared generated and loaded geology contains sparse deterministic loose pixels of Iron Ore, Copper, Aluminum, and deeper Uranium throughout sand, soil, silt, mud, and stone. Gold is explicitly excluded from random ground distribution and remains authored/special. Deposits never replace sky, authored scenes, foundations, shells, lava, or the lava cap. CPU and GLSL layouts use identical hashing/rates; packaged visual and recovery acceptance remain required. v2.5.6 clustered-vein evidence: minerals now form deterministic 32x24-host-region elliptical clumps with rough low-frequency edges, sharply reducing mixed/broken terrain tiles while preserving Iron Ore > Copper > Aluminum > deep Uranium abundance and excluding Gold. Resident mud and ore veins now inherit durable structural metadata from their host geology so deposits no longer tear stable tiles apart or create false air/liquid voids. v2.5.13 tranche-one evidence: terrain-generation and shader contracts revalidate deterministic Iron Ore/Copper/Aluminum/Uranium deposits, Gold exclusion, trap flags, stable host geology, and CPU/GLSL agreement; packaged visual/recovery remains active. |
| MC-123 | REGRESSION | Cross-system packaged runtime acceptance | The first 28 P0 systems run together in packaged Windows/Linux builds without stale hierarchy metadata, frozen productive motion, lost media, broken authored origins, malformed ground, incorrect pause/reset clocks, missing hives, or UI input leakage. A seeded acceptance sequence cycles every scene, exercises Half Water and full Water, disturbs gas/liquid with the player, runs machinery and bees, toggles pause/map, resets repeatedly, and save-loads the result while conserving represented state. Deterministic contracts may reduce this gate but cannot close its packaged visual/runtime portion. v2.5.13 restores this missing active row, maps all first-28 P0 systems to deterministic tests/audits, and adds CI source validation. The seeded packaged Windows/Linux scene-cycle, Half Water, machinery, bee, pause/reset, and save/load sequence remains required before closure. v2.5.14 packaged runtime contradiction: moving macro tiles and Half Water were still observably incorrect, so this gate is explicitly reopened; v2.5.15 source recovery changes production ownership and wake paths, but packaged observation remains required. v2.5.17 focused evidence: the focused Windows packaged Vulkan sequence now accepts exact macro liquid/gas movement, blocked fine fallback, Half Water production/motion/merge, and generated hive contents. The complete every-scene, player, machinery, bee-lifecycle, pause/map/reset, save/load, Linux, and conservation sequence remains active. |
| MC-124 | REGRESSION | Tile-first deposits with trap-bound loose resources | Natural Iron Ore, Copper, Aluminum, and deep Uranium veins are authored as complete stable 8x8 structural tiles with rough clustered outlines. Loose resource cells are never decorative confetti: they exist only inside deliberate collapsible sand-trap roofs/backfill above a cavity, remain mixed with loose Sand, and can fall to bury or expose the player. CPU and GLSL generation must match exactly; every loose resource sample carries the sand-trap flag, and no partial structural deposit tile is permitted. Packaged visual/collapse acceptance remains required. |
| MC-113 | PARTIAL | Exact directional production transactions | Every industrial machine defines input material, power/medium requirements, processing latency, output material, input port, output port, blocked-output behavior, and a player-switchable direction where meaningful. One accepted transaction produces its documented goods on the output side or consumes nothing. Sluice transactions follow MC-088 exactly. v2.5.5 core evidence: `machinery.hpp` validates recipes, power/medium, direction, blocked output, and atomic consume/produce behavior; runtime machine components, latency, UI, and debug binding remain. v2.5.13 tranche-one evidence: `sandhybrid_machinery_contract` re-proves directional ports, blocked-output rollback, recipe requirements, and atomic consume/produce behavior; runtime latency/UI binding remains active. |
| MC-107 | PARTIAL | Component actors and directional machinery | Player, insects, conveyors, sluices, smelters, assemblers, vents, and habitats use occupancy/components outside material identity. Machines expose configurable input/output ports, consume explicit input cells, produce explicit output cells on the opposite side, support player-switchable output sides/directions where applicable, and report accepted/rejected transactions. v2.5.5 core evidence: Core actor occupancy, medium state, directional ports, atomic machines, deterministic sluicing, and habitat policy now exist; Vulkan actor/machine migration remains. v2.5.13 tranche-one evidence: actor-medium and machinery contracts revalidate component actors, medium separation, directional ports, and atomic transactions; Vulkan component binding remains active. |
| MC-108 | OPEN | Sparse 512x512 stream pages | Eight-by-eight section pages allocate on demand, load before cross-page transfer, save modified distant pages asynchronously, and evict clean pages. Large mostly-static worlds avoid fixed full-world allocation and scanning. |
| MC-109 | OPEN | Section-driven Vulkan runtime | Vulkan consumes immutable section batches and dirty rectangles from the core, dispatches only active work, retains a CPU reference path, and reports separate simulation/debug/presentation timings. Shader code does not own world policy. |
| MC-110 | OPEN | Deterministic cutover and old-hardware gate | Old and replacement runtimes run identical seeded scenes and compare material totals, gas components, moisture, heat, damage, actors, and machine outputs. One-million-active-cell and mostly-static-large-world baselines pass on Windows/Linux and representative older four-core hardware before old hierarchy code is deleted. |

## Simulation hierarchy and settling

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-011 | PARTIAL | 64x64 section-first rejection | `SparseSectionGrid` now provides dirty rectangles, safe phases, halo wakeups, and sleeping metadata. The production runtime must consume those batches so clean sections execute zero tile/fine/material work while pressure and boundary exchange remain safe. Runtime profiling proves it. |
| MC-012 | REGRESSION | 8x8 bulk-element movement | Full aligned liquids, gases, powders, mud, and wet granular materials use the same gravity, diagonal, lateral, density, erosion, and displacement rules as fine cells. Wet Sand/Silt clears stale structural state when wetted and can sink through liquids even during macro-only wet-material scheduling. Block-capable solids never translate as whole tiles. Static contracts pass; packaged wet-sand and non-zero bulk-move observation remains required. v2.5.13 evidence: complete exposed liquid/gas tiles receive a macro transaction attempt, structural and Half Water cells remain excluded, and the unchanged/blocked path remains fine-active for same-tick fallback. Headless/source contracts pass; packaged non-zero bulk movement and wet-sand observation remain required. v2.5.14 packaged runtime contradiction: a fine-active uniform Air target vetoed an otherwise exact Water packet, while intact macro packets could also be fragmented by ordinary fine passes. v2.5.15 source recovery removes that target veto and restores fine-pass exclusion unless either tile actually requests fine fallback. v2.5.17 focused evidence: the Windows Release packaged Vulkan state-readback observes a conserved 64-cell Water packet move exactly one 8x8 tile, an exact 64-cell Hydrogen move, and a blocked Water packet retaining 64 cells while eight cells take fine fallback. Powder, mud, wet-granular, erosion, and packaged Linux/broader scene observation remain active. |
| MC-100 | PARTIAL | Solid tiles never move wholesale or auto-crumble | Full stone and other block-capable regions may report `BULK READY` and sleep through tile metadata, but no macro dispatch may swap them and no occupancy/support heuristic may release their surviving structural cells. `CELLS` remain loose; `TILES` remain durable until each represented cell is directly destroyed or changes physical phase. Sand, soil, silt, salt, and ice retain granular stability qualification without granting block-capable reconstruction. Static contracts pass; packaged runtime acceptance remains required. |
| MC-013 | REGRESSION | True liquid settling | Water levels quickly, reaches zero motion, and wakes only from support, pressure, volume, heat, reaction, actor, or tool disturbance. Exposed full liquid packets perform one fine-ownership breakup, then an unchanged packet sleeps on the next tile pass instead of cycling for 48 frames. v2.5.6 screenshot-driven fix also prevents stable half-water tiles from remaining permanently fine-active. Packaged observation remains required. v2.5.13 evidence: exposed complete Water packets now attempt macro motion before fine fallback, while unchanged boundary media can settle after the one-time breakup. Headless/source contracts pass; packaged leveling and zero-motion observation remain required. v2.5.14 packaged runtime contradiction reopens this mission; v2.5.15 protects committed/intact packets from non-fallback fine fragmentation while retaining cell-level fallback for mixed and partial boundaries. v2.5.17 focused evidence: the exact Water packet and blocked fine-fallback checks pass in the Windows production Vulkan pipelines. Leveling to zero motion, wake causes, exposed breakup/rest, and long-running packaged observation remain active. |
| MC-084 | REGRESSION | Half-water conserved fine behavior and card identity | Inspected fractional fresh water is titled `HALF WATER`. Half Water never enters macro transfer. Creation remains supplied and conserved; two halves consolidate deterministically; an isolated half may move only a bounded two-to-four-cell distance toward another visible half, matching the early v2.4.1 curve-forming behavior, and never wanders randomly. Unchanged halves sleep without checkerboard activity. Packaged observation remains required. v2.5.13 fixes ambient-Air restoration to the canonical 54-state Atmosphere value and prevents Half Water from sleeping while a clear partner is two to four cells away. Half Water remains excluded from macro ownership. Packaged ledge, attraction, consolidation, and card observation remain required. v2.5.14 packaged runtime contradiction: age-only rest could sleep Half Water while a fall or neighboring merge was still available. v2.5.15 adds explicit fall/merge wake predicates and restores the v2.4.1 guard against diagonal half-cell wandering. v2.5.17 focused evidence: Windows Release GPU readback preserves one half through falling, merges two clear-gap halves into one full Water cell without unit loss, and creates exactly two half cells from the accepted supplied-ledge branch. Card identity, broader curve formation, and Linux visual acceptance remain active. |
| MC-014 | PARTIAL | Fractional-water consolidation | Fractional water represents only final surface volume; no lateral splitting loop, chasing, jitter, created air, or isolated pockets. |
| MC-015 | PARTIAL | Hide hierarchy artifacts | Fine repair prevents squares, popping, seams, grid-edge clumping, and diagonal one-cell ramps. |
| MC-016 | OPEN | Durable hierarchy terminology | Rename fine cells, 8x8 bulk elements, 64x64 sections, active window, frozen regions, and streamed regions in code, UI, debug, and docs together. |
| MC-017 | REGRESSION | Prevent premature stabilization without blanket activation | Liquid, mud, wet material, and gas sleep only after material-specific equilibrium. Fresh enclosed Atmosphere/Oxygen tiles enter settled macro sleep immediately; exposed full gas packets perform one fine handoff and then sleep when unchanged. `ACTIVE TILES` measures actual work rather than the active-area gate. v2.5.6 adds the one-pass exposed-medium rest contract; packaged observation remains required. v2.5.13 evidence: Half Water with a visible two-to-four-cell partner remains active instead of sleeping from age alone, and exposed media preserve fine fallback after failed macro work. Packaged activity-count and equilibrium observation remain required. v2.5.14 packaged runtime contradiction reopens this mission because Half Water could enter settled sleep with productive fall/merge work pending; v2.5.15 source policy blocks that premature rest. v2.5.17 focused evidence: productive Half Water fall/merge/attraction and blocked packet fallback pass GPU state readback. Activity-count accuracy and material-specific long-running equilibrium remain active. |
| MC-018 | PARTIAL | Bulk/fine parity tests | Deterministic tests prove each eligible 8x8 move matches the equivalent conserved fine-cell result, including Atmosphere displacement, mud erosion, gas boundary flow, and the same rest decision. v2.5.5 core evidence: A contract proves one full 8x8 Water packet exactly matches 16,320 represented fine transfers and blocked commits mutate neither side; broader material/runtime parity remains. v2.5.13 expands deterministic policy contracts for macro-first/fine-fallback decisions, Half Water rest/attraction, and pause clocks in addition to the exact packet transaction suite. Broader production material parity remains active. v2.5.17 focused evidence: the Windows Release production pipelines now prove exact Water/Hydrogen packet displacement and conserved blocked Water fine fallback from resident-buffer readback. Atmosphere, mud, erosion, gas-boundary, rest-decision, and Linux parity remain active. |
| MC-019 | REGRESSION | Material-specific settling without gas-wall friction | Damping is not a universal per-move slowdown. Gravity, buoyancy, pressure transfer, mud erosion, falling wet material, reactions, boundary changes, tool input, and actor impulses execute at the full material-defined rate. Liquids and mud may use viscosity/internal drag only after no productive move exists, reducing residual lateral oscillation toward rest. Gases have no friction against solids: a wall blocks the normal crossing direction but never adds tangential slowdown or pins gas to the surface. Gas motion ends only through pressure/composition equilibrium, density sorting, or exhausted impulse. Bulk and fine representations make the same active/rest decision. |
| MC-078 | PARTIAL | Authored structures remain intact | Authored beams, walls, machines, tanks, hives, platforms, metals, and ores retain structural state cell by cell until direct destruction or a real physical conversion affects that cell. Missing neighbors, low occupancy, minority-material classification, and lack of four cells directly underneath never auto-crumble block-capable construction. Runtime scene cycling must show no spontaneous disassembly. |
| MC-053 | REGRESSION | Transient gas/liquid tile packets | A complete 8x8 gas or liquid region may move as one tile packet. After packet motion ends, gas remains tile-owned only when its complete one-cell perimeter is gas; liquid remains tile-owned only when its complete perimeter contains liquid or non-gas matter. Exposed resting packets become `FINE_ACTIVE` and break back to canonical cells without deleting, filling, snapping, or changing volume. Runtime proof must show moving air/water tiles, exposed breakup, enclosed retention, and stable zero-motion results. v2.5.5 core evidence: Exact all-or-fallback represented packet transactions and packet/fine parity now exist; production ownership/perimeter/breakup integration remains. v2.5.14 packaged runtime contradiction reopens this mission. v2.5.15 permits exact displacement into a full uniform fine-active Air tile, marks successful packets before the fine pass, and skips intact macro pairs unless a side explicitly requires fine fallback; packaged moving-packet/breakup observation remains required. v2.5.17 focused evidence: Windows Release readback accepts non-zero exact Water and Hydrogen packet moves and a blocked same-tick fine fallback without Water loss. Perimeter ownership, exposed breakup, enclosed retention, stable zero-motion, and Linux/broad packaged proof remain active. |

## Atmosphere and closed-system gas

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-020 | REGRESSION | Composite Atmosphere | An append-only `Atmosphere` material represents normal authored air with breathable oxygen rather than aliasing pure Oxygen. v2.5.6 smooths balanced-air presentation and stops settled exposed full-gas packets from repeatedly advertising breakup while preserving per-cell composition. Packed production storage, pressure exchange, saves, shaders, and rendering migration remain active. |
| MC-021 | PARTIAL | Earth-air baseline | Authored air and the Atmosphere action now use a distinct Atmosphere state with 54/255 (~21.2%) breathable oxygen rather than pure Oxygen. Exact packed N2/O2/Ar/CO2/trace composition, pressure totals, and conservation tests remain required. v2.5.5 core evidence: `make_earth_atmosphere` creates an exact 65,535-unit baseline with 54/255 O2, ~0.9% Ar, trace CO2/Ne, and N2 remainder; authored/runtime initialization remains. |
| MC-022 | PARTIAL | Absorb all true gases into air | Gas painting and emissions modify local Atmosphere composition and pressure instead of replacing it. v2.5.5 core evidence: `enrich_atmosphere` changes packed composition while preserving full-cell pressure and other components; production paint/emission paths still need binding. |
| MC-023 | OPEN | Visible excess-gas settling | No nested mini-simulation. Excess denser than air visibly moves down then sideways until stable; lighter excess visibly moves up then sideways. It cannot cross solids, liquids, sealed barriers, paused sections, or unloaded boundaries. Solids block crossing but do not create tangential gas friction: gas slides freely along valid open boundary paths at its density/pressure-defined rate. |
| MC-024 | OPEN | Reabsorb excess, hide only later | Stable excess reabsorbs when compatible air has capacity. Transit remains visible until Adam accepts it; debug always exposes transport afterward. |
| MC-025 | PARTIAL | Respiration and combustion | Life and combustion convert available O2 to equal represented CO2 volume. Suffocation uses breathable partial pressure. Runtime rates require proof. v2.5.5 core evidence: Core respiration converts packed O2 to equal CO2 without changing pressure and actor contracts derive suffocation from fraction/demand; production life/combustion rates remain. |
| MC-026 | PARTIAL | Closed-box conservation tests | Track total Atmosphere and each component, pressure transfer, separated excess, reabsorption, and conservation error. Include wall-following tests proving that gas contact blocks penetration without slowing valid lateral/upward/downward transport. v2.5.5 core evidence: Contracts prove total and per-component conservation across transfer, O2-to-CO2 respiration, and pressure-preserving enrichment; multi-cell boundary/shader tests remain. v2.5.13 tranche-one evidence: `sandhybrid_atmosphere_contract` re-proves total/component conservation, respiration, enrichment, and transfer; production multi-cell wall-following and packaged observation remain active. |
| MC-027 | OPEN | Composition rendering | Balanced air renders smoothly; validation builds show excess transport clearly; accepted builds may later hide normal transit. |
| MC-028 | OPEN | Validate corner pressure structures | Preserve corner patterns only when conserved pressure/composition produces them. Reject structures created by gas-wall friction, sticky corners, or solid-adjacency sleep. |
| MC-029 | PARTIAL | Atmosphere inspection/tools | The large Atmosphere action writes the distinct append-only Atmosphere state, Oxygen remains independently selectable, Eraser writes vacuum, and cards distinguish those identities. Pressure and per-component gas percentages still require implementation and runtime acceptance. v2.5.5 core evidence: Core inspection exposes exact pressure and oxygen-per-mille plus component mapping; UI component percentages and production composition painting remain. |
| MC-079 | PARTIAL | Uniform atmosphere edge equilibrium and truthful activity count | A completely Atmosphere-filled resident map reaches zero gas-edge activity and full enclosed Atmosphere/Oxygen tiles sleep on the first reset frames. Finite world edges are sealed. `ACTIVE TILES` excludes clean enclosed air and reports only genuinely active tile work. Real excess gas remains active until pressure/density equilibrium. Static contracts pass; packaged startup observation remains required. v2.5.13 tranche-one evidence: shader/source validators revalidate sealed edges, settled enclosed Air classification, truthful active-work policy, and canonical 54-state balanced Air; packaged startup counters remain active. |
| MC-126 | REGRESSION | Separate Air flood fill from Ignite Air | `FILL` and the `F` modifier always replace the cursor-connected target region with balanced `Atmosphere`/Air, independent of the selected material. The Heat category exposes a distinct `IGNITE AIR` action that finds the upper-leftmost Atmosphere cell in row-major world order and replaces only that connected Air region with Fire. The utility button and non-card UI say `AIR`; the inspected material card retains the canonical `ATMOSPHERE` title. Deterministic source/UI contracts and Windows/Linux packaged observation are required. v2.5.15 source evidence: the sidebar FILL control arms one operation without mutating; the next confirmed world left-click supplies the target and shares the same fixed Atmosphere flood operation as F+left-click; packaged observation remains required. |
| MC-127 | REGRESSION | Half Water isolated from ambient Air bookkeeping | Half Water remains a supplied ledge-volume state and cannot be displaced, rewritten, activated, or visually corrupted by balanced Air. When a full Water cell splits into Half Water against Air, Air is stored only as a zero-pressure ambient marker and restores as canonical Air on consolidation; no hidden Atmosphere pressure occupies Half Water aux state. Non-Air excess gases remain conserved. Packaged ledge/consolidation observation remains required. |
| MC-129 | REGRESSION | Same-material falling cells repair damaged tiles | A loose cell contacting an existing damaged structural tile of the same material is consumed into that tile column: it fills a represented hole or restores a damaged structural cell to full health. It never creates an unrelated standalone tile, repairs a different material, or bypasses the existing-tile requirement. `FINE REPAIR` counts accepted repairs. Packaged impact/mining observation remains required. |
| MC-130 | REGRESSION | Stable liquid pools, vacuum expansion, and Smoke buoyancy | Acid and other low-viscosity liquids run outward from supplied surfaces and settle into level basin pools rather than pillars or isolated vertical strings. Balanced Air expands pressure-conservatively into adjacent vacuum until local pressure is exhausted. Smoke is lighter than Air and rises through it rather than sinking. Windows/Linux packaged basin, vacuum, and buoyancy observation remains required. |

## Life, ecology, and player interaction

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-030 | REGRESSION | Remove life from fine swaps | Bees, ants, beetles, queens, and player use actor/occupancy state, never exchange material records, stand on hierarchy edges, or count as fine swaps. |
| MC-031 | PARTIAL | Actor occupancy and medium overlap | Actors overlap air/liquid independently, conservatively displace media, breathe local composition, and can drown or suffocate. v2.5.5 core evidence: `ActorOccupancy` and `MediumState` separate actors from gas/liquid identity; contracts preserve pressure/liquid while deriving respiration, drowning, and suffocation; runtime buffers remain. v2.5.13 tranche-one evidence: `sandhybrid_actor_medium_contract` re-proves independent actor occupancy, conserved displacement, respiration, drowning, and suffocation decisions; Vulkan runtime binding remains active. |
| MC-032 | PARTIAL | Complete bee lifecycle | Forage, pollen, Beehive return, deposit, honey feeding, queen/Beehive aging, migration, hazards, respiration, replacement, and the 100-bee autonomous cap pass multi-cycle runtime testing. Generated and loaded authored bees now encode/decode home positions through the shared crystal-row Y=720 origin instead of targeting the retired bottom-row origin. Multi-cycle packaged observation remains required. v2.5.13 tranche-one evidence: ecology and Fix34 audits validate the 100-bee cap, current packed swarm data, queen/home routing, and lifecycle source paths; multi-cycle packaged observation remains active. |
| MC-033 | REGRESSION | Recurring readable biohazard | Formation is recognizable, dominant, recurring, and free of grid-edge clumping or premature colony death. |
| MC-034 | OPEN | Real ant behavior | Colonies, pheromone trails, forage/carry/home behavior, hazards, flooding, and permitted digging replace generic particle wandering. |
| MC-035 | OPEN | Real beetle behavior | Beetles crawl surfaces/walls, seek food/shelter, respond to light/hazards, and turn at obstacles without flying/jittering as particles. |
| MC-036 | PARTIAL | Define or remove insect habitat | Give it explicit species, capacity, inputs, lifecycle, and outputs, or remove it; no generic silent spawning. v2.5.5 core evidence: `InsectHabitatPolicy` explicitly supports Ant/Beetle species, capacity, food, water, waste, and cadence and rejects generic Bee spawning; runtime controller/UI remain. |
| MC-037 | PARTIAL | Life debug counters | Debug reports actor moves and species counts for bees, queens, Beehives, ants, beetles, habitats, flowers, pollen, and honey. Respiration, suffocation, births, deaths, Beehive returns, and medium displacement require separate counters and runtime acceptance. |
| MC-038 | REGRESSION | Exact pre-PR19 suspended Beehive prefab | Material save ID 31 is Beehive. Generated Sandbox and Ecosystem scenes, the build tool, and loaded-map normalization share the exact hard-coded Ecosystem hive restored by SimpleSandSim Fix36 from immediately before PR #19: local queen centers (512,234) and (512,232), loose Wood perch offsets x=-37..29 and y=-16..-13, shell 25 <= radius² < 92, chamber radius² < 25, queen at zero, exit x=1..10 with |y|<=1, and deterministic Empty/Honey/Pollen contents from the two-bit chamber rule. SandHybrid retains its own 100-bee formation, lifecycle, and home metadata; no SimpleSandSim bee behavior or population is imported. Source contracts now cover reset, build, load migration, structure classification, and contents. Packaged reset/place/load/save visual observation remains required. v2.5.17 focused evidence: the Windows Release reset pipeline reproduces the canonical Ecosystem hive with zero cell mismatches: 268 support cells, 209 shell cells, 18 Honey, 22 Pollen, 16 empty chamber cells, queen, and exit. Reset/tool/load now share canonical Ecosystem-local entropy; packaged tool/place/load/save and Linux observation remain active. |
| MC-039 | PARTIAL | Player-medium impulses | Player collisions minimally disturb every gas/liquid through bounded conserved directional impulse, wake touched sections, and then settle through MC-019 only after the injected impulse and resulting productive movement are exhausted. v2.5.5 core evidence: Bounded signed medium impulses now preserve gas pressure and liquid representation and clamp deterministically; runtime collision sampling and section wakeup remain. v2.5.13 tranche-one evidence: `sandhybrid_actor_medium_contract` re-proves bounded pressure/liquid-preserving impulses and deterministic clamping; collision sampling and packaged settling remain active. |

## UI and debug

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-040 | PARTIAL | Selectable balanced Atmosphere tool | The always-visible `ATMOSPHERE` control selects the distinct Atmosphere state with ~21% breathable oxygen for ordinary painting. It never triggers a region fill and is not vacuum, pure Oxygen, or an alias. Packaged runtime acceptance and full component packing remain required. |
| MC-041 | PARTIAL | Distinct terrain eraser | Keep an adjacent always-visible `ERASER` that selects vacuum/empty deletion. It remains visibly and mechanically distinct from `ATMOSPHERE` and the selectable `OXYGEN` material. Packaged runtime acceptance remains required. |
| MC-042 | PARTIAL | Clarify movement counters | Use `FINE SWAPS`, `BULK MOVES`, `BULK CELLS`, `FINE REPAIR`, `ACTOR MOVES`, `GAS EXCESS`, `PLAYER IMP`, `GAS TILES`, `LIQUID TILES`, `ENCLOSED TILES`, and `BREAKUP TILES`. Runtime values must correspond to actual work and ownership transitions. |
| MC-043 | PARTIAL | Preserve lower GPU use | Timestamp overlay, grid, text, stats, bulk, fine, Atmosphere excess, actors, collision impulses, and presentation independently. |
| MC-044 | PARTIAL | Remove grid-edge coupling | Debug grid and relational state colors are presentation-only fragment-shader overlays and are not read by simulation shaders. Deterministic runtime comparison with debug enabled/disabled remains required. |
| MC-045 | PARTIAL | High-contrast readable debug UI | Preserve the accepted current text size. Categories use distinct high-contrast colors, tile/chunk and active-region boundaries remain readable, and the color key exactly matches damaged, stable, bulk-moved, fine-active, bulk-ready, settled, sleeping, active, enclosed, and breakup overlays. |
| MC-046 | REGRESSION | Debug never blocks the world | On ordinary layouts the complete debug panel occupies the existing sidebar instead of covering the simulation viewport. The world remains visible and interactive; no full-screen stats rectangle is permitted. Very small windows may hide or page nonessential counters, but may not cover the simulation. |
| MC-085 | REGRESSION | Pair-test and skipped-work debug samples | `PAIR TESTS` and `SKIPPED` preserve the last completed simulation sample. Render-only frames never clear them to zero. Pair tests reflect actual fine/macro pair dispatch scope and skipped work reflects real tile/chunk rejection. |
| MC-080 | PARTIAL | Resource-first activity debug | Keep the accepted UI layout and text size. Sort resource-critical metrics first: FPS, resident cells, active map areas, tested pairs, skipped work, active/sleeping hierarchy. Follow with meaningful activity by cause: fine/bulk motion, gas/liquid transport, structural failures, conveyors, machine input/output, volcano lava/gas, chemistry, actors, and ecology. `FINE ACTIVE` is vivid/hot while `SLEEPING` is dark/cool and their legend order shows active -> settling -> sleeping. Runtime readability remains required. |
| MC-125 | REGRESSION | Truthful complete debug hierarchy telemetry | The sidebar reports scope cells, nonempty cells, structural/liquid/gas cells, total/active/sleeping/unclassified tiles, fine/bulk/settled tiles, and total/active/sleeping/dirty chunks from the actual live buffers. `TOTAL TILES = ACTIVE TILES + SLEEP TILES + UNCLASSIFIED` for every completed sample, total chunks bound the chunk-state rows, and movement labels match their real counters (`FINE SWAPS`, `BULK MOVES`, `BULK CELLS`, `FINE REPAIR`, `GAS EXCESS`, `PLAYER IMP`). Render-only frames preserve the last completed sample. Packaged screenshot acceptance remains required. |
| MC-128 | REGRESSION | Non-overlapping wider sidebar stack | The sidebar is widened modestly, the keymap owns enough vertical space for every row, and the Cursor editor plus material card begin below it with explicit non-overlap. The CPU hit-test layout and fragment-shader drawing use the same heights and widths at 1920x1080 and compact supported sizes. Packaged screenshot acceptance remains required. v2.5.15 source evidence: UI/renderer pointer mapping is shared, preview radius/shape equals committed paint, logical input geometry remains distinct from physical framebuffer presentation during resize/high-DPI display, and sidebar pointer movement hides rather than edge-clamps the world outline; packaged screenshot/feel acceptance remains required. v2.5.17 focused evidence: a direct Windows Release capture on a 150% display shows the world cursor centered over its paused committed Sand edit and no clamped world ghost after the pointer enters the sidebar. Additional wide/compact stacks and feel/hit-test coverage remain active. |
| MC-131 | REGRESSION | Exact readable debug state legend | The color key lists the actual overlay precedence in plain state names: damaged, active, fine active, bulk moved, bulk ready, breakup, settled, enclosed, sleeping, and stable. Each has a strongly separated hue/brightness, chunk colors reuse dirty/active/sleeping semantics, and world overlays are restrained tile edges so material identity remains readable. Packaged screenshot acceptance remains required. |
| MC-092 | PARTIAL | Soil-facing terminology | Player-facing cards, palette labels, documentation, and UI call material ID 3 `SOIL`; internal IDs remain stable for saves and shader compatibility. Static contracts pass; packaged runtime inspection acceptance remains required. |
| MC-093 | PARTIAL | Distinct Atmosphere, Fill, Oxygen, and Eraser controls | `ATMOSPHERE` selects the balanced-air material, `F FILL` fills with the current selection, `OXYGEN` remains selectable pure gas, and `ERASER` selects vacuum. Their labels, colors, hit regions, and actions are never aliases. Packaged runtime UI acceptance remains required. |
| MC-094 | PARTIAL | Debug state cards and distinct ready/steady colors | The bottom of debug info contains readable cards with a large actual color block plus state name. `BULK READY` is vivid violet and `SETTLED` is vivid green; all relational states remain distinguishable in hue and brightness. Packaged screenshot acceptance remains required. |
| MC-095 | PARTIAL | Default square size-two brush | Fresh startup defaults to square brush shape and size 2 through named shared-state defaults without changing later user selection semantics. Packaged startup acceptance remains required. |
| MC-096 | PARTIAL | Restore Windows launcher | Root `run.bat` searches packaged and common Release build locations, launches `sandhybrid.exe`, forwards arguments, reports failures, and is installed into the Windows package root. Packaged Windows acceptance remains required. |
| MC-098 | PARTIAL | Restrained GUI group separation | Thin dividers separate scene navigation, editing controls, resource metrics, activity metrics, event counters, and debug state cards. Separation clarifies hierarchy without dense boxed clutter or reduced text size. Packaged visual acceptance remains required. |
| MC-099 | REGRESSION | Separate Air/Fill action with reusable selection bounds | `ATMOSPHERE` selects balanced Air and `ERASER` selects vacuum. `F FILL` executes only on a confirmed left-click. With no selection it fills only the cursor-connected target region; with an active rectangular selection it is strictly clipped to that box. The same marquee geometry and hit testing are shared with blueprint copy/paste, and no keyboard press alone edits the world. Packaged bounded-fill and input acceptance remain required. v2.5.15 source evidence: the previously inert FILL button now arms a one-shot connected Air fill whose target requires the next confirmed world left-click; rectangular selection clipping remains active work. |
| MC-119 | PARTIAL | Explicit pause, camera/player input, and non-modal map | The top GUI provides live `RUNNING`/`PAUSED`, `PLAYER WASD`/`WASD PAN`, and `MAP` controls. MAP toggles a non-modal overview overlay instead of replacing the simulation view. Character scenes keep player WASD unless `WASD PAN` is selected; non-player scenes route WASD to the simulation camera. The overlay owns only pointer interaction inside its own rectangle, with separate zoom and right-drag pan state. Static routing/layout contracts pass; packaged click, motion, and pause observation remains required. v2.5.13 additionally freezes MAP snapshot refresh while paused, preserving the last complete overview while camera/map/UI navigation remains available. Packaged click, motion, and pause observation remains required. |
| MC-120 | PARTIAL | Right-click captured drag and gated edge panning | Right mouse exclusively owns camera drag and edge panning in every scene and map mode. Edge movement occurs only while right-click is held near a viewport edge; ordinary pointer movement never pans. Captured drag keeps direct view-to-pixel scaling and sub-cell remainder outside the viewport. Middle mouse performs no camera action. Right-click never paints Oxygen, erases, mines, or deposits. Static routing/source contracts pass; packaged feel and capture observation remains required. v2.5.13 tranche-one evidence: input-routing contracts revalidate right-button-only drag/edge pan, player/camera separation, map-overlay pointer ownership, and absence of right-click editing; packaged feel/capture remains active. |
| MC-047 | PARTIAL | Universal cell-or-tile placement | Every selectable material routes through the explicit `CELLS` / `TILES` selector and aligned tile painting path while retaining canonical material IDs and wakeup. Machine metadata, actor exclusions, and runtime parity still require broader acceptance. |

| MC-121 | PARTIAL | Independent readable map overlay and world-design view | MAP toggles a centered aspect-correct overview over the still-running simulation instead of replacing it. The slow snapshot preserves actual material colors, water, hives, structures, terrain layers, restrained active-region outlines, and the live camera rectangle. Map pan/zoom remain independent and pointer-scoped; main camera, player input, painting outside the overlay, and simulation scheduling remain unchanged. Static overlay/render/input contracts pass; packaged clarity, refresh, and world-layout acceptance remain required. |
| MC-122 | REGRESSION | Sidebar Inventory with Inventory and Blueprints sub-tabs | Inventory exists only in the normal sidebar body. It exposes nested `INVENTORY` and `BLUEPRINTS` tabs, four honest empty blueprint slots, the shared material card, and no fabricated 2x2 metal stockpile. Neither sub-tab opens a center, floating, or modal window. Blueprint slots preserve authored cell state, preview identity, dimensions, and placement rules; packaged sidebar/hit-test acceptance remains required. v2.5.16 source/runtime evidence: Inventory renders four shared slots with real occupied/empty/selected state and activates placement only for occupied slots; a Windows Release frame shows the PAUSED world still visible beside the `INVENTORY`/`BLUEPRINTS` sidebar with slot 1 occupied and slots 2-4 empty. The legacy resource-pane presentation and broader packaged hit-test cycle remain active. |

## Scene authoring and repository hygiene

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-115 | REGRESSION | Paint-editable scene image palette and legacy load migration | Save, Load, `material_key.txt`, and `material_key.ppm` use one stable RGB table. Exact colors round-trip; nearby colors map to the nearest material. Screenshot evidence showed legacy Empty sky loading as a giant vacuum/status rectangle. A legacy image with no represented Atmosphere/Oxygen now flood-migrates only boundary-connected Empty to Atmosphere while preserving sealed vacuum pockets, then rebuilds the common resident substrate. Packaged Paint edit/load observation remains required. v2.5.13 tranche-one evidence: scene-image contracts revalidate stable palette round trips, nearest-color migration, boundary-connected legacy-Air repair, structural metadata, and queen/hive reconstruction; packaged Paint/load remains active. |
| MC-116 | PARTIAL | Canonical backlog and one brief changelog | `missioncache.md` contains each unfinished requirement once with priority and acceptance criteria, not per-release prose. `CHANGELOG.md` is the only release-history file. Versioned release-note files, one-shot patch payloads, obsolete workflows, and completed agent branches are removed before publication. CI validates the cache and release tree. v2.5.13 tranche-one evidence: mission-cache validation now rejects orphaned or duplicate P0 references, MC-123 is restored, versioned release-note fragments and checked-in obsolete Fix33 packages are removed, and CI packages the canonical cache/changelog/current notes; publication-tree verification remains active until release. |

## Chemistry and materials

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-050 | PARTIAL | Correct fertilizer chemistry | Ember becomes only fire or ash, never fertilizer. A mutually paired compost reaction requires visible ash, organic waste/food, silt/dirt/mud, dirty water, oxygen, and time; one feed cell becomes fertilizer while one dirty-water cell becomes clean water. Runtime conservation and rate acceptance remain required. |
| MC-086 | REGRESSION | Tile-mode terrain placement and soil stability | `TILES` mode paints aligned structural 8x8 packets for sand, soil (internal material ID 3), silt, salt, ice, and all block-capable materials. Soil tiles remain stable while supported and release only through damage, lost support, phase change, or the 48%-destroyed collapse threshold. Released block-capable solids move only as fine cells. |
| MC-087 | REGRESSION | Conserved shallow wet sand | Sand/silt absorb water only in a bounded shallow surface band. Once wet, the water remains represented in the wet granular cell and cannot disappear through a timer-based dry flag. |
| MC-088 | REGRESSION | Vertical wet-feed sluice output | A vertical, water-supplied sluice accepts one wet sand or wet silt cell at a time. Each accepted feed has a deterministic seeded 10% gold roll: success outputs exactly one Gold cell and one Water cell; failure outputs exactly one original Sand/Silt cell and one Water cell. Solid and water outputs leave through separate sides, and the player can switch those output sides/directions. Dry feed is rejected; input, roll, output, rejection, and conservation counters remain visible. v2.5.5 core evidence: Core sluicing rejects dry/invalid feed, requires Sand/Silt plus Water, applies a seeded 10% roll, separates outputs, and commits nothing when blocked; production wiring remains regressed/unverified. |
| MC-097 | PARTIAL | Derived wet material variants | Moisture-retaining solids and powders expose derived `WET <MATERIAL>` card identity and darker/cooler wet color treatment without separate palette tools. Wet states arise only through physical mixing, absorption, reaction, or loaded state. Sand remains slightly hydrophobic; wet sand gains explicit density and sinks through ordinary liquids. Static contracts pass; packaged mixing/sinking acceptance remains required. v2.5.13 tranche-one evidence: behavior contracts revalidate wet-density increase and wet-sand policy while macro/fine fallback retains wet material activity; packaged physical mixing and sinking remain active. |
| MC-051 | PARTIAL | Wet-material, mud-erosion, and sluicing proof | Wet sand/dirt/silt and mud prove full-speed gravity-driven bulk descent and erosion through unsupported material. Wet granular water remains represented until an explicit conserved evaporation/transfer path moves it; timer-based flag deletion is forbidden. A vertical water-supplied Sluice Box processes each wet sand/silt cell exactly once, performs the seeded 10% MC-088 gold roll, separates water from solid output, respects switched output directions, and never stalls because of hierarchy damping. |
| MC-081 | REGRESSION | Local conserved volcano output | Screenshot evidence showed the volcano as a static vent pillar that stopped because Atmosphere blocked the empty-only outlet and open vents drained pressure without recharge. The chamber now stores Lava, crater/floor nozzles are Magma Vents, gas/Atmosphere outlets accept eruption output, pressure recharges locally, and each event emits Lava or hot Ash/Smoke/Steam immediately above the vent. Ordinary gas activity elsewhere never creates lava. Packaged sustained-eruption and conservation proof remains required. |
| MC-082 | PARTIAL | Functional industrial machinery | Powered conveyors visibly transport loose cargo and expose a player-switchable travel/output direction. Smelters, assemblers, and other industrial equipment consume only documented matching input cells and emit the documented goods from the opposite output port; blocked output prevents consumption. Vertical sluice boxes implement MC-088 and expose switchable separated output sides. Habitat controllers process explicit inputs. Machines stay simulation-active, never consume without matching output, and debug reports direction changes, conveyor moves, machine input/output, blocked output, and rejected feed. v2.5.5 core evidence: Core machine and sluice transactions now reject blocked outputs without consumption and support separate switchable outputs; visible runtime execution and counters remain. |
| MC-052 | REGRESSION | Settled granular terrain becomes structural | Dry reconstructable dirt, sand, silt, salt, and ice remain loose while sliding. After at least 52 represented cells in an 8x8 region remain unchanged and physically supported for the stabilization window, those existing cells gain structural and supported state without filling or snapping missing cells. Loss of support, fewer than 32 cohesive cells, mining, heat/phase change, player impact, or renewed motion clears structural state and restores crumbling. Side bracing may support terrain but is not required for floor-supported piles to stabilize. Runtime proof must show the pictured brown slopes settle solid and later release into slides when disturbed. |

## World, camera, scheduling, streaming, and concurrency

The canonical active shape is a **contiguous 4x4 map-footprint window** derived from the live simulation camera. One active region is the complete pre-expansion map size: **640x360 cells**, never a 64x64 chunk and never an 8x8 tile. The window is clipped and shifted at world edges so it retains up to 16 complete regions without sparse gaps.

Runtime profiling may reduce the active window only through an explicit visible quality mode; it may never silently reinterpret the region size in chunk or tile units. The default 4x4 window is the release contract.

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-060 | PARTIAL | Camera-visible state availability | The complete bounded resident 4x4 map-footprint window is allocated and renderable at supported zoom levels, including paused state. Far logical regions beyond the resident window remain blocked on MC-063 streaming and runtime visibility acceptance. |
| MC-061 | PARTIAL | Camera-centered contiguous 4x4 map-area window | Animate one clipped and edge-shifted 4x4 rectangle of complete 640x360 map-footprint regions around the simulation camera. The window contains up to 16 regions, prefers one region behind/left and two ahead/right when unconstrained, and never uses sparse ring spokes. Code/debug treat 64x64 chunks and 8x8 tiles as lower hierarchy levels only. Packaged profiling must prove realistic motion and bounded GPU cost. |
| MC-062 | PARTIAL | Hard pause outside active map areas | Outside MC-061: no movement, chemistry, ecology, actors, Atmosphere transport, pressure propagation, or simulation-debug work. Wake deterministically upon entry. Frozen regions remain renderable and cannot corrupt authored maps. |
| MC-063 | OPEN | Far-section disk streaming | Serialize clean distant paused sections, keep only a bounded resident GPU window, free buffers where appropriate, and reload deterministically with versioned corruption-safe saves. This is required before the full logical 8x8 world may be resident without MC-077 risk. |
| MC-064 | PARTIAL | Automatic active-region worker scheduler | Reserve the main thread completely for windowing, input, rendering coordination, and everything non-region-related; simulation jobs never run there. Determine hardware concurrency automatically. Use up to 16 simulation workers, one per active 640x360 map-area region when at least 17 hardware threads exist including the reserved main thread. With fewer than 17, pair additional regions onto workers from the furthest regions first. Never oversubscribe, never schedule paused regions, and preserve deterministic boundary results. Current scheduler assignment exists; actual independent region execution remains runtime-unverified. |
| MC-066 | OPEN | Safe pause/wake/boundary transfer | Resolve pending transfers, actors, impulses, pressure, and dependencies before pause. Crossing the active-window edge conserves all state without allowing the paused side to animate. |
| MC-067 | PARTIAL | Reshape the 64-footprint world to 16x4 | The resident/logical world is 16 authored-map footprints wide by 4 high: 10240x1440 cells. It preserves the former 64-footprint total, extends only to the right, places the complete authored 640x360 scene at aligned region origin `(1280,720)`, and updates generation, buffers, indexing, saves, map framing, limits, overflow checks, camera reset, bee homes, and player spawns together. Static coordinate and layout contracts pass; packaged memory/performance observation remains required. |
| MC-068 | PARTIAL | Camera home/reset | Every scene starts on the complete authored footprint at aligned region origin `(1280,720)`. Reset returns to exactly that 640x360 view and one-map scale without touching simulation state. The full MAP view frames the complete 16x4 world at map zoom 1. Runtime verification remains required. v2.5.13 tranche-one evidence: input-routing, map-overlay, and world-layout contracts revalidate aligned camera home/reset and independent map framing; packaged camera behavior remains active. |
| MC-069 | PARTIAL | 2x2 maximum zoom-out | Maximum zoom-out displays exactly 1280x720 cells: four 640x360 footprints in a 2x2 view. Default/reset displays exactly 640x360. Minimum/default/maximum zoom values scale with the resident window, preserving pointer mapping, panning, active-scope clarity, and culling. |
| MC-074 | PARTIAL | Authored objects and actors use one aligned scene origin | Every authored object, saved scene pixel, hive home, machine, metadata record, camera home, actor reset, and player spawn preserves its local coordinate inside one complete 640x360 region at aligned world origin `(1280,720)`. Generated and loaded scenes use the same offset. Static scene/player/actor contracts pass; packaged scene cycling/load acceptance remains required. v2.5.13 tranche-one evidence: world-layout and scene-image contracts revalidate the shared `(1280,720)` origin across authored pixels, player/actor coordinates, hives, and generated/loaded substrate; packaged scene cycling remains active. |
| MC-075 | PARTIAL | Camera navigation and scope HUD | Right-click drag and right-click-held edge panning work at every zoom; middle mouse is unused. The live `PLAYER WASD`/`WASD PAN` control owns character-scene keyboard routing, while non-player scenes always use camera WASD. The independent map camera has separate center/zoom state and never changes the active-region center or simulation LOD. The sidebar shows zoom, active-region mode/count, and map/debug controls; world debug draws map-area boundaries. Packaged drag, edge, reset, toggle, HUD, and boundary rendering remain runtime-unverified. |
| MC-089 | PARTIAL | Upgrade every scene to the shared resident geology stack | Sandbox, Blank, Volcano, Waterworks, Ecosystem, Engineering, Gold Mine, Demolition, and Frontier Base use the same two-row sky volume, continuous resident-width surface and stone foundation, lower sand/soil/silt/mud/stone geology, stone lava cap, two-brick lava band, and stone bottom shell without changing authored local coordinates. Generated reset and legacy PPM load now preserve every non-empty authored object while filling empty authored ground from the shared substrate. Packaged visual/save-load acceptance remains required. |
| MC-114 | PARTIAL | Common four-footprint vertical world layout | The first two 640x360 resident rows are empty sky and the authored scene occupies world Y=720. The lower geology now places soil above sand, uses deterministic cell-scale jitter and mixed transition cells instead of ruler-straight layer boundaries, and scatters irregular mud pockets rather than uniform 64x64 mud blocks. CPU and GLSL generation share the policy. Reset and Load produce the same substrate. Static contracts pass; packaged visual/save-load acceptance remains required. v2.5.13 tranche-one evidence: world-layout and terrain-generation contracts revalidate two sky rows, the Y=720 authored footprint, continuous substrate, layered geology, and CPU/GLSL policy parity; packaged visual acceptance remains active. |
| MC-090 | PARTIAL | Scientific experiment layouts | Engineering uses controlled thermal, single-aperture gas-diffusion, sediment-separation, and paired compost control/treatment experiments. Gold Mine includes a visible water-fed conveyor/sluice station. Inputs, controls, treatment, and outputs are spatially distinguishable without relocating existing objects. Runtime observation remains required. |
| MC-091 | PARTIAL | Explain fertilizer mechanics in-world | Ember and ash/fertilizer cards state that ember becomes ash only and fertilizer requires compost ingredients. Engineering contains a clean-water control and dirty-water compost treatment so the mechanic explains itself through visible inputs and different outcomes. No hidden ember-to-fertilizer shortcut remains. |
| MC-077 | PARTIAL | Windows GPU memory-manager crash in tile mode | Selecting `TILES` never allocates, resizes, or rebinds GPU memory. The logical world remains 8x8 map footprints, but the resident GPU window is bounded to 4x4 map footprints until MC-063 implements deterministic streaming. Windows/Linux CI must pass; Windows runtime must prove repeated CELLS/TILES switching, painting, reset, scene cycling, save/load, and debug use without device loss, TDR, WDDM reset, allocation growth, or stale descriptors. |

## Library architecture

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-071 | PARTIAL | Thin `SandHybrid_Demo` | `SandHybrid_Demo` owns native startup/events and links the optional in-tree `SandHybrid::VulkanRuntime`; Vulkan renderer code no longer enters the core archive. Exported runtime/native-host embedding acceptance remains required. |
| MC-072 | PARTIAL | Optional subsystems | `SANDHYBRID_BUILD_APP=OFF` with `SANDHYBRID_BUILD_VULKAN_RUNTIME=OFF` builds, tests, installs, and externally consumes a clean core package without configuring Vulkan, EpochGui, shaders, windowing, or native event sources. Independent switches for streaming, debug, UI, actors, ecology, and factories remain open. v2.5.5 core evidence: New Atmosphere, inventory, packet, actor-medium, and machinery APIs remain platform-neutral installed headers with no Vulkan/EpochGui leakage; independent subsystem switches remain. |
| MC-073 | DEFERRED | EpochEngine integration | Later migrate/rewrite using canonical `epochengine::` APIs while preserving reusable `SandHybrid` boundaries. |


## Runtime screenshot evidence — v2.5.6 release blockers

These observations are authoritative contradictions. A release cannot erase a row, mark it accepted from compilation alone, or fix only one propagation target.

| Evidence | Visible contradiction | Linked missions | Required propagation and acceptance |
|---|---|---|---|
| EV-256-001 | Water and Half Water form persistent 8x8 checkerboards and visibly stop at ownership boundaries. | MC-012, MC-013, MC-014, MC-015, MC-017, MC-053, MC-084 | Fine/macro ownership, tile/chunk sleep, map/debug presentation, save/load, and packaged settling must agree. |
| EV-256-002 | Balanced Atmosphere shows coarse active/broken blocks and can stop or churn at boundaries. | MC-017, MC-019, MC-020, MC-026, MC-027, MC-079 | Gas movement, pressure, tile/chunk sleep, counters, map/debug rendering, and packaged equilibrium must agree. |
| EV-256-003 | MAP paints a dominant checkerboard over the world instead of showing readable regions and camera scope. | MC-044, MC-045, MC-080, MC-121 | MAP suppresses tile/chunk status paint; it shows only restrained active-region and main-camera outlines at 4 Hz. |
| EV-256-004 | The authored scene straddles a camera-region split and retains internal vertical scene walls. | MC-067, MC-068, MC-074, MC-089, MC-114, MC-123 | Origin `(1280,720)` propagates to reset, load/save, camera, actor reset, player spawn, bee homes, metadata, and tests; only outer perimeter walls remain. |
| EV-256-005 | Load produces a giant solid/vacuum-looking rectangle and discards expected open Atmosphere presentation. | MC-077, MC-114, MC-115, MC-123 | Legacy boundary-connected Empty migrates to Atmosphere, sealed vacuum remains, resident substrate/shell rebuilds, and packaged Save→Load round-trip is visually identical. |
| EV-256-006 | Volcano is a static maroon conduit/pillar, emits weakly or not at all, then stops. | MC-080, MC-081, MC-123 | Chamber is Lava, nozzles are Magma Vents, Atmosphere/gas outlets accept local eruption output, pressure recharges, counters advance, and packaged eruption remains sustained without unrelated lava creation. |
| EV-256-007 | Ash hangs in air far too long and appears to float like gas. | MC-012, MC-017, MC-019, MC-050, MC-081, MC-123 | Ash remains a particulate powder, falls through gases at a high cadence, may be ejected hot by vents, and settles without being frozen by stale hierarchy metadata. |
| EV-256-008 | Materials, liquids, powders, or gases sometimes stop despite an available productive move. | MC-011, MC-012, MC-017, MC-018, MC-053, MC-061, MC-062, MC-066, MC-123 | Failed macro transactions fall back to fine movement in the same tick; chunk sleep skips only sleep-safe pairs; active-window edges preserve state and wake deterministically. |
| EV-257-001 | Debug screenshot shows hierarchy counters without total tile/chunk scope and movement labels that do not identify the underlying counters, making missing/unclassified work impossible to diagnose. | MC-042, MC-080, MC-085, MC-125 | Debug reads the live cell/tile/chunk buffers, exposes complete scope totals and classification remainder, uses exact counter names, and preserves the last completed sample while paused/rendering. |
| EV-257-002 | Loose resource cells were described as decoration rather than a deliberate collapse hazard. | MC-105, MC-112, MC-118, MC-124 | Structural veins remain complete tiles; every loose resource cell belongs to a flagged sand trap above a cavity and can collapse with Sand to bury/expose the player. |
| EV-258-001 | `FILL` can inherit Fire and ignite the connected atmosphere instead of restoring Air; no separate world-seeded ignition action exists. | MC-020, MC-029, MC-126 | Cursor fill has a fixed Air replacement; Heat has a separate `IGNITE AIR` action seeded from the upper-leftmost Air cell; card identity remains Atmosphere. |
| EV-258-002 | Half Water presentation/settling is disturbed by Atmosphere bookkeeping. | MC-013, MC-014, MC-017, MC-084, MC-127 | Ambient Air is a marker rather than hidden pressure inside Half Water; consolidation restores canonical Air and non-Air gases remain conserved. |
| EV-258-003 | Sidebar text overlaps and the Cursor/material-card stack begins too high. | MC-042, MC-085, MC-125, MC-128 | Sidebar width increases, keymap height fits all rows, and CPU/shader panel boundaries agree without overlap. |
| EV-258-004 | Loose same-material cells stop on damaged tiles while only later settling can create a separate tile. | MC-034, MC-039, MC-105, MC-129 | Contact repairs the existing matching tile column immediately, fills holes before health, consumes exactly one loose cell, and increments `FINE REPAIR`. |
| EV-258-005 | Acid forms implausible columns/strings, Air leaves connected vacuum black, and Smoke sinks through Air. | MC-012, MC-017, MC-020, MC-024, MC-130 | Supplied low-viscosity liquid surfaces level into basins, Air splits represented pressure into vacuum, and Smoke density remains below Air density in CPU/GLSL contracts. |
| EV-258-006 | Debug state swatches and world colors no longer communicate which hierarchy state they represent. | MC-044, MC-045, MC-080, MC-094, MC-125, MC-131 | The legend follows overlay precedence, uses ten distinct named colors, and restricts hierarchy tinting to tile edges so cells remain identifiable. |


## v2.5.9 regression recovery, rendering, and usability

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-132 | REGRESSION | Material-by-material historical behavior audit | Compare every material's first accepted implementation and the first two follow-up releases against current movement, chemistry, settling, rendering, tile eligibility, and sleep rules. Generalized powder/fall-pattern optimizations may remain only where parity is proven. Each material has an explicit profile and deterministic test; no material silently inherits Powder behavior because of a broad category helper. Runtime visual acceptance remains required. |
| MC-133 | REGRESSION | Restore early Half Water dynamics | Use v2.4.1 as the behavioral baseline: Half Water stays fine-owned, conserves half-units and displaced medium, merges deterministically, receives its dedicated passes, and may make a bounded short attraction move toward another visible half to form smooth curves. It never becomes an 8x8 packet or a checkerboard overlay. v2.5.15 source recovery keeps unsupported or merge-ready halves awake and prevents supported Half Water from taking a diagonal random-flow path; packaged visual observation remains required. v2.5.17 focused evidence: the production Vulkan gate passes fall-first, bounded clear-gap attraction/merge, conserved half-units, and supplied-ledge creation. Broader packaged curve/sleep observation remains active. |
| MC-134 | REGRESSION | Cohesive Lava and cyclic volcanic pressure | Lava remains a hot dense semi-solid, moves continuously between dry Soil and Mud behavior, prefers connected roughly tile-sized masses, and only isolated cells cool. Vents pulse on/off over a multi-minute cycle, recharge faster, culminate in a larger eruption, propagate pressure through connected Lava, and fracture or lift overburden until connected to open Air/sky. The Volcano scene contains a natural curved lake basin, not a framed tank. |
| MC-135 | REGRESSION | Smoke paint conserves displaced media | Painting Smoke into Water, Half Water, Acid, any liquid, or any gas never deletes represented volume. The contacted medium moves upward through its column; displaced gas pressure transfers into compatible gas cells; solids block insertion; failed displacement performs no write. GPU and reference contracts prove conservation. |
| MC-136 | REGRESSION | Restore the early suspended Beehive | Generated Sandbox/Ecosystem hives and the Beehive tool use the approved pre-PR19 Ecosystem geometry restored in Fix36: exact queen centers, wide loose-Wood support, 25..91 shell, sub-25 chamber, 1..10 exit, queen, deterministic honey/pollen/empty contents, and the SandHybrid swarm formation. Bee home metadata follows the restored queen center and survives reset/save/load. Completion requires packaged reset/build/save/load observation. v2.5.17 focused evidence: the Windows Release generated Ecosystem readback accepts the exact hard-coded support, shell, queen, exit, and chamber-content pattern with zero mismatches. Packaged Beehive tool, save/load metadata, colony lifecycle, and Linux observation remain active. |
| MC-137 | REGRESSION | Acid corrosion never manufactures Silt | Silt is produced only by sediment and dirty-water separation mechanics. Acid corrosion damages structural cells and converts organics to Waste or dissolved inorganic material to Dirty Water; it never uses Silt as a generic destruction product. Acid remains a level pooled liquid. |
| MC-138 | REGRESSION | Exact sidebar workspace ownership with isolated designer controls | Settings owns global/top controls. Editor owns world scene/material/cursor/cells-tiles/fill/camera editing. Inventory and Designer render only inside the normal sidebar body; each exposes nested `INVENTORY` and `BLUEPRINTS` tabs and neither may open a center, floating, or modal window. Inventory contains honest blueprint slots and no fabricated resources. Designer repeats the material palette and cursor/cells-tiles/shape/size/zoom controls for its isolated sidebar grid and never mutates world-editor state. Static Model and Map Chunk authoring remain Designer-owned. The material card is visible in every workspace and hidden controls cannot receive input. Packaged visual/hit-test acceptance is required. v2.5.15 source evidence: Editor/Designer retain isolated brush state and sidebar-only controls; world pointer/preview policy is shared and tested, while packaged ownership/hit-test acceptance remains required. |
| MC-139 | REGRESSION | Color-first restrained material rendering | Cell identity and physics remain authoritative. Normal presentation uses stable material coloring and only the minimum transparency or motion cue needed to distinguish media. Remove stacked glint, curved timing, flow-band, opacity, and noise effects that obscure water, hives, terrain, or debug inspection. Any optional cosmetic effect must be individually gated, render-only, provenance-independent, and visually accepted before becoming a default. |
| MC-140 | PARTIAL | World-size presets and complete dimension telemetry | Debug reports horizontal cells, vertical cells, total cells, horizontal tiles, vertical tiles, and total tiles. Camera-compatible Compact 4x4, Standard 8x4, and Large 16x4 footprint presets preserve 640x360 authored/camera regions; runtime selection, memory budgeting, and older-system acceptance remain open. |

### v2.5.9 screenshot/runtime evidence

| Evidence | Runtime contradiction | Linked missions | Acceptance propagation |
|---|---|---|---|
| EV-259-001 | Screenshots show Half Water and Air as large checkerboard/macro-looking blocks rather than smooth conserved media. | MC-012, MC-013, MC-017, MC-053, MC-084, MC-133 | Full uniform Water/Air may use validated macro packets; Half Water remains fine-only and restores the early bounded attraction/consolidation behavior. Rendering never exposes packet ownership as opaque squares. |
| EV-259-002 | Lava stalls, breaks into powder-like cells, and fails to stay in cohesive tile-scale masses. | MC-012, MC-019, MC-132, MC-134 | Lava bypasses sparse powder timing, remains continuously active, uses fine cohesion plus full-tile macro motion, and only isolated cells cool. |
| EV-259-003 | Magma Vents emit too slowly and do not create natural pressure-driven volcanic eruptions. | MC-081, MC-123, MC-134 | Vents pulse on/off for minutes, recharge quickly, enter a major-eruption window, pressurize connected Lava, and open a route through loose/damaged overburden toward Air/sky. |
| EV-259-004 | The Volcano scene's rectangular framed pool does not read as a real lake. | MC-068, MC-074, MC-123, MC-134 | The scene uses a wide curved cell-resolution basin with shallow natural banks and a consistent waterline. |
| EV-259-005 | Painting Smoke into liquid erases liquid and fails to displace gas upward. | MC-014, MC-022, MC-135 | Column insertion shifts liquid upward, transfers displaced gas pressure, preserves Half Water state, blocks at solids, and performs no write when conservation cannot be satisfied. |
| EV-259-006 | The UI lacks workspace tabs, top controls lack category descriptors, and material cards contain excessive `N/A`. | MC-042, MC-128, MC-138 | Functional workspace tabs, visible section descriptors, shared hit/draw geometry, and `NONE`/live material values replace ambiguous presentation. |
| EV-259-007 | Material glint and timing differ between real-time creation, paused state, and falling ore; per-cell noise produces poor patterns. | MC-044, MC-139 | Render-only tile-coherent appearance profiles use a uniform render clock and curved local coordinates independent of cell age/provenance. |
| EV-259-008 | Debug lacks width/height/total cell and tile dimensions needed to understand map scale and compare world presets. | MC-080, MC-125, MC-140 | Debug exposes six dimension counters and the API defines camera-compatible Compact, Standard, and Large resident footprints. |
| EV-259-009 | Current Beehives no longer match the approved suspended hive from the early FastFreddy/SandHybrid ecology scene. | MC-050, MC-074, MC-136 | Restore the wooden support, compact shell/chamber/exit, queen, honey/pollen, swarm formation, and matching home metadata. |
| EV-259-010 | Generic Acid corrosion turns unrelated material into Silt, conflating chemistry with sediment generation. | MC-050, MC-130, MC-137 | Generic corrosion produces Waste or Dirty Water; only dirty-water separation and sediment paths produce Silt. |
| EV-259-011 | Broad optimized fall categories caused previously distinct materials to inherit Powder behavior and regress over time. | MC-012, MC-018, MC-019, MC-132 | Historical per-material audit identifies the first divergent release and restores explicit motion/settling profiles with parity tests before any generalized fast path is retained. |

## v2.5.12 screenshot/runtime direction — 2026-08-05

| Evidence | Runtime direction | Linked missions | Required propagation and acceptance |
|---|---|---|---|
| EV-2512-001 | MAP must behave like the approved top-centered overview: toggle on/off without replacing the main view, preserve proper aspect/material rendering, and retain independent zoom/pan. | MC-119, MC-121 | Overlay and main camera remain simultaneously visible and independently controlled; overlay input is clipped to its own bounds and never changes active scheduling. |
| EV-2512-002 | Cursor controls remain in Editor for world editing. Inventory and Designer are sidebar-only workspaces with nested `INVENTORY` and `BLUEPRINTS` tabs. Designer receives a separate copy of the palette and cursor/cells-tiles/shape/size/zoom controls for its isolated sidebar grid, multi-material static models, and captured map chunks that publish into blueprint slots. | MC-122, MC-138, MC-141, MC-149, MC-150 | Editor and Designer control state never bleeds across workspaces; fake inventory resources stay removed; no center/floating/modal inventory or designer body exists; exact capture/thumbnail/preview/paste remains active until runtime accepted. |
| EV-2512-003 | The existing sunlight buffer should produce visible light and a day/night cycle rather than remaining plant-only hidden infrastructure. | MC-031, MC-151 | Daylight follows simulation time, sunlight occlusion affects presentation and plants, emissive/artificial lighting remains visible at night, and time persists through save/load. |
| EV-2512-004 | Every scene needs the continuous ground shown in the approved map screenshot, without cutting the substrate at the authored 640x360 footprint or erasing authored structures. | MC-089, MC-114, MC-143 | Reset and PPM import composite authored non-empty cells over one continuous full-width surface/foundation/geology stack; Blank and every authored scene receive ground. |
| EV-2512-005 | PAUSED freezes material motion/effects, particles, actors, physics, lighting time, and day/night progression, but world editing remains live: paint, erase, Fill, Ignite Air, selection operations, and blueprint paste commit immediately without advancing simulation. RESET restarts cells, particles, actor state, hierarchy/debug data, mineral effects, map snapshot, camera/map framing, lighting, and the visual clock. | MC-068, MC-099, MC-119, MC-139, MC-151, MC-152 | Presentation uses simulation time; paused edits are visible immediately, execute no simulation step, and do not queue delayed duplicate actions; reset clears/rebuilds all dependent buffers before the first reset render. |

## Backlog review policy

Every P0 mission is attempted in the current release. Every P1/P2 row is reviewed for contradiction and retained until its own acceptance passes. Static compilation never closes visual, runtime, conservation, or performance work.

## v2.5.10 terrain, liquid, workspace, and fracture corrections

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-141 | REGRESSION | Workspace-specific sidebar hierarchy | Global controls never have captions drawn through them. Inventory, Editor, Settings, and Designer render distinct bodies only in the sidebar and hidden controls cannot receive input. Inventory and Designer each provide nested `INVENTORY` and `BLUEPRINTS` tabs; no Designer or Inventory body may occupy the main viewport, a center overlay, a floating panel, or a modal window. Editor and Designer may show parallel palettes/cursor controls only through separate state and hit-test paths: Editor targets the world; Designer targets its isolated sidebar grid. Packaged visual acceptance remains required. |
| MC-142 | PARTIAL | 47% structural fracture and compact tool bursts | Complete structural tiles collapse on the 31st destroyed cell, release every survivor into down/diagonal movement, and cannot repair while collapsing. Tool visuals are short sparse pixel bursts. Static contracts pass; packaged visual/physics acceptance remains required. |
| MC-143 | PARTIAL | Continuous terrain and coherent geology | Ground continues across every resident-world column, including beneath the authored footprint. Non-empty authored structures, basins, and terrain composite over the shared substrate instead of being overwritten; empty authored cells inherit the same surface/foundation/geology as neighboring regions. Mineral cores remain complete structural tiles, broken material is limited to localized rubble/trap pockets, and biome boundaries transition through broad deterministic bands. Static contracts pass; packaged map acceptance remains required. |
| MC-144 | PARTIAL | Volcano lake/lava recovery and low-friction liquids | Volcano visibly contains a water lake, crater lava lake, reservoir, throat, vents, cone, and gas space. Liquids conserve volume while falling, equalizing, and flowing over ledges without the legacy 18-frame cutoff. Shader compilation and static contracts pass; packaged motion acceptance remains required. |

## v2.5.11 exact world persistence and size-aware folders

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-145 | PARTIAL | Replace broken gameplay scene-image saves with exact world saves | Save and Load operate on the entire selected-size resident world, preserving every `SceneCell` material, age, temperature, and aux field. The versioned 64x64 chunk stream chooses raw or RLE encoding, verifies each chunk and the full payload, validates dimensions/scene before mutation, publishes atomically, retains one backup, and recovers from a damaged primary. Deterministic contracts pass; packaged Windows/Linux save-load observation remains required. |
| MC-146 | PARTIAL | Terraria-style selectable world sizes | Compact 2560x1440, Standard 5120x1440, and Large 10240x1440 allocate matching Vulkan buffers before startup. `--world-size` and the supplied size launchers select the preset, and incompatible saves cannot crop or stretch into another size. A polished in-window new-world selector remains open. |
| MC-147 | PARTIAL | Durable save folder and slot layout | Saves live under `saves/worlds/<size>/<scene>/<slot>/` with sanitized named slots, `world.shw`, `world.bak`, and `manifest.txt`. PPM remains a 640x360 authored-scene asset format only. Package folder portability and multi-slot runtime acceptance remain required. |
| MC-148 | REGRESSION | Save/load runtime reliability | Repeated save, overwrite, load, backup fallback, scene switching, and all three world sizes must preserve exact state without crashes, truncation, stale tile/chunk metadata, or loading into the wrong scene. Static and headless contracts exist; packaged stress acceptance remains active. |

## v2.5.13 tranche 1 — first 28 P0 missions

This pass attempts the first 28 P0 IDs in listed order: `MC-012`, `MC-013`, `MC-017`, `MC-018`, `MC-026`, `MC-031`, `MC-032`, `MC-038`, `MC-039`, `MC-068`, `MC-074`, `MC-079`, `MC-084`, `MC-097`, `MC-104`, `MC-105`, `MC-106`, `MC-107`, `MC-112`, `MC-113`, `MC-114`, `MC-115`, `MC-116`, `MC-118`, `MC-119`, `MC-120`, `MC-122`, and `MC-123`.

### Pre-change impact and risk review

- Half Water, full Water, packet/fine ownership, active/sleep state, and represented gas volume share `aux` bits; changes must preserve save compatibility, never allow Half Water into macro movement, and never hide balanced-Air pressure inside Half Water.
- Bulk movement and fine fallback must not translate structural tiles, duplicate cells, delete displaced gas, or leave a failed packet dormant until the next frame.
- Atmosphere conservation, actor displacement, bee lifecycle, machinery transactions, and player impulses must remain component/volume conserving and deterministic across worker counts.
- Camera/reset/origin changes must not move authored scene coordinates, bee homes, player spawn, save coordinates, or the non-modal map overlay.
- Ground/geology continuity must extend through every world column without overwriting the aligned authored footprint, water, hives, structures, lava shell, or deliberate sand traps.
- Pause must freeze every simulation-derived presentation clock and reset must clear every queued/derived effect without disabling camera, map, or UI navigation.
- Inventory remains blueprint-slot based; Designer authoring controls are isolated from Editor world controls even when both expose analogous cursor/material operations.
- `MC-123` is restored as the packaged cross-system runtime acceptance gate that ties these interdependent fixes together instead of leaving an orphaned P0 reference.

### Tranche acceptance

1. Add deterministic reference coverage for Half Water attraction/consolidation/rest, liquid packet fallback/rest, atmosphere wall-following conservation, actor-medium impulses, machinery atomicity, world continuity, scene-origin parity, and pause/reset clocks.
2. Update production CPU/GLSL paths where contracts expose a mismatch; no source-only token test may substitute for state/result assertions when a platform-neutral reference is possible.
3. Run the complete headless C++ suite, shader/source validators, mission-cache validation, and an inspectable full diff.
4. Windows and Linux native Release builds/tests/package publication remain mandatory before the tag is called released. Packaged visual items stay `PARTIAL` or `REGRESSION` until observed.

## v2.5.14 macro-tile, Fix29 hive, pause-editing, and sidebar recovery handoff

### Pre-change impact and risk review

- Packaged SandHybrid v2.5.3 is the accepted macro-tile baseline. Its complete liquid/gas tiles qualify for macro work while moving or enclosed; v2.5.7 first restricted that path to enclosed non-moving media, and the current `!moving` predicate still prevents visibly moving complete media from taking the known-good path.
- Restore the v2.5.3 moving-or-enclosed liquid/gas qualification without reverting later same-tick fine fallback, one-pass exposed-medium breakup/rest, structural-tile exclusion, Half Water fine-only ownership, exact packet transactions, or conserved gas/liquid displacement.
- The old task chain identifies SimpleSandSim Fix36 as the authoritative correction: it explicitly restored the hard-coded Ecosystem hive immediately before PR #19. Use its visible hive body and contents only: queen centers (512,234)/(512,232), wide loose-Wood perch offsets x=-37..29 and y=-16..-13, shell 25 <= radius² < 92, chamber radius² < 25, right exit x=1..10 with |y|<=1, and deterministic Empty/Honey/Pollen fill. Do not import SimpleSandSim bee orbit/movement, population, material IDs, world layout, or other runtime behavior.
- Propagate that exact hard-coded hive through generated Sandbox/Ecosystem reset, the Beehive painting prefab, PPM normalization/migration, structural classification, tests, and validators while retaining SandHybrid authored-home metadata and current bee lifecycle.
- PAUSED continues to stop simulation, actor, particle, material-animation, map-snapshot, lighting, and day/night clocks. Editor mutations remain enabled and commit immediately against the frozen world without advancing time or being replayed after resume/reset.
- Inventory and Designer remain sidebar workspaces. Each owns nested `INVENTORY` and `BLUEPRINTS` sub-tabs; no center/floating/modal Designer or Inventory surface is permitted, and Editor/Designer control state remains isolated.
- User-visible releases use ordinary stable version tags and GitHub releases without a prerelease marker or `-test` suffix. Internal validation builds remain untagged workflow artifacts or drafts so the newest public release remains visible in ordinary update/release surfaces.
- Affected active missions are MC-012, MC-013, MC-017, MC-018, MC-038, MC-053, MC-099, MC-116, MC-119, MC-122, MC-123, MC-136, MC-138, MC-141, MC-150, MC-152, and MC-153. Visual/runtime missions remain active until packaged observation.

### Implementation evidence (source stage)

- Macro ownership: `shaders/tiles.comp` now uses the v2.5.3 moving-or-enclosed predicates; `tests/behavior_contract.cpp` asserts moving exposed gas/liquid eligibility and stationary perimeter compatibility, while existing packet tests retain atomic all-or-fallback behavior.
- Hive parity: shaders/beehive.glsl, shaders/reset.comp, src/scene_image.cpp, and actor-medium contracts now share the Fix36 pre-PR19 Ecosystem shell 25..91, chamber <25, exit 1..10, wide support, exact map queen positions, and deterministic chamber contents while retaining SandHybrid-owned bee behavior.
- Pause/edit separation: `editor_mutation_allowed` is a deterministic policy used by the renderer; paint, Fill, and Ignite Air no longer reject paused input, while simulation clock, actor, MAP, sunlight, and effect gates remain paused.
- Sidebar ownership: `ui_layout.hpp` defines Inventory tabs plus Designer grid/card sidebar rectangles; `app.cpp` routes Inventory slots and Designer paint to those rectangles; `fullscreen.frag` renders both sidebar bodies and no longer substitutes Designer for the world viewport; layout tests assert sidebar containment.
- Stable publication: `AGENTS.md`, `MISSION_LEDGER.md`, release docs, and the tag-gated workflow require normal `v2.5.14` publication with Windows/Linux archives and SHA-256 files; automatic branch deletion and public test-release publication were removed.
- Source gate passed: release-tree, canonical shader/interface, v2.5.10-v2.5.14 recovery, ecology/Fix34 audits, mission-cache, no-coroutine, branding, workflow-YAML, and clean-diff validation.
- Windows package evidence: MSVC 19.44 Release compiled the application and all 12 GLSL shaders with warnings-as-errors; all 24 CTest targets passed; install, ZIP, SHA-256, and archive-content audit completed.
- Linux package evidence: native GNU 11.4 x64-linux Release compiled the application and all 12 GLSL shaders with warnings-as-errors; all 24 CTest targets passed; install, tar.gz, SHA-256, and archive-content audit completed.
- Both archives contain the platform executable, 12 SPIR-V shaders, library/development files, launchers, canonical release notes, and mission ledger. v2.5.14 was published at https://github.com/Autodidac/EpochSimEngine/releases/tag/v2.5.14 from commit `71d6098ce6ec99bc89475af6859382a2b31ce9a4` as a normal release. Windows SHA-256: `cc80807b3afd1560c973b17753aea0792450026f4b8f169e99798a86d0329d5c`; Linux SHA-256: `cf8ec6c96480a974230b77e81242e067cf2fbaa4fa21f01db4cb0aae86092e81`. Subsequent packaged observation contradicted macro-tile and Half Water acceptance, so affected runtime missions remain active.

### Recovery acceptance

1. Deterministic source/contracts prove moving complete Water and Atmosphere packets are macro-eligible, exposed failed transactions remain fine-active in the same tick, enclosed unchanged media can settle, and Half Water/structural cells never enter macro movement.
2. CPU and GLSL hive contracts use the same Fix36 pre-PR19 hard-coded body/content/support constants and deterministic chamber rules across reset, paint, legacy scene-image normalization, and actor classification.
3. Paused editing contracts prove each authorized edit mutates the frozen world exactly once while simulation/presentation time and physics remain unchanged; reset drops queued one-shot input.
4. Layout and input contracts prove Inventory and Designer use sidebar-only bodies with nested `INVENTORY`/`BLUEPRINTS` tabs at supported window sizes.
5. Run the complete headless C++ suite, shader/source validators, mission-cache validator, and inspect the complete diff for unrelated behavioral changes.
6. Build and test native Windows and Linux Release packages. Publish only a normal visible release after packaged acceptance; never close visual/runtime missions from static evidence.

## v2.5.15 macro ownership and Half Water runtime recovery

### Packaged contradiction and risk review

- v2.5.14 compiled, packaged, and published correctly, but that evidence did not prove visible simulation behavior. Packaged user observation reports that macro tiles and Half Water still do not match their accepted historical baselines.
- A full uniform Air tile at a Water boundary may be marked `TILE_FINE_ACTIVE`. That scheduling flag must not veto an otherwise exact atomic 8x8 displacement; mixed, partial, structural, or already-moved targets still reject the transaction.
- Intact macro tiles must not be fragmented by the ordinary fine pass. Fine fallback remains live in the same tick whenever either side is genuinely `TILE_FINE_ACTIVE`.
- Half Water must not sleep while it can fall, while a neighboring half can merge, or while a clear half is two to four cells away. Supported Half Water cannot take the generic diagonal Water-flow path.
- These source corrections reopen MC-012, MC-013, MC-017, MC-038, MC-053, MC-084, MC-099, MC-123, MC-126, MC-128, MC-136, and retain MC-133/MC-138 as REGRESSION. They do not close packaged visual/runtime acceptance.

### Source-stage evidence

- `shaders/macro_move.comp` treats fine-active as scheduling metadata and accepts full uniform displaceable targets; `shaders/move.comp` restores v2.5.3 intact-macro exclusion only when neither tile needs fine fallback.
- `shaders/tiles.comp` derives Half Water fall, neighboring-merge, and bounded-attraction work before declaring rest; `shaders/move.comp` restores the early full-Water-only diagonal ledge guard.
- `simulation_policy.hpp` and `behavior_contract.cpp` provide platform-neutral assertions for fine-active exact targets, partial/mixed rejection, macro/fine ownership, and Half Water rest denial for fall/merge work.
- Beehive recovery: the old task chain and Fix36 tag identify the exact pre-PR19 Ecosystem model; reset, paint, load normalization, actor classification, validators, and repository instructions now use that model without importing foreign bee behavior. Cursor/fill recovery: CPU and renderer share pointer-to-grid and effective brush policies, logical pointer mapping remains distinct from physical framebuffer presentation during resize/high-DPI display, sidebar hover hides the world outline, and sidebar FILL arms a one-shot operation whose target requires the next world left-click. Windows and Linux Release build/package evidence and packaged user observation must be appended here before publication or acceptance claims.
- v2.5.15 package evidence (2026-08-05): native Windows Release and native Linux Release each pass 24/24 tests after the high-DPI cursor correction. Fresh installs carry all 12 SPIR-V shaders and canonical package documents. Packaged Windows visual observation shows the normal sidebar beside the live Ecosystem world, the suspended hive at its hard-coded authored location, a world cursor at the actual pointer location, and no clamped world-edge cursor while the pointer is in the sidebar. Macro-tile movement, Half Water motion, hive chamber contents, pause-editing, and the complete cross-system scene cycle remain active runtime acceptance rather than being closed from this screenshot.
## v2.5.16 paused character-scene editing and real Blueprint slots

### Risk and impact review

- An occupied Blueprint must never share a click with paint, Fill, mining, or resource deposit. Empty slots must remain visibly and mechanically inactive.
- Static models need canonical fresh cell state at their final world indices; copied Map Chunks must retain exact `SceneCell` material, age, temperature, aux, and deliberate Empty cells.
- Rotation and mirroring change the complete footprint. Bounds and every material ID must validate before either resident GPU cell buffer changes.
- PAUSED paste must commit without advancing simulation time. Reset/load is a hard boundary that discards queued paste instead of replaying it.
- Renderer slot reads and Designer publication occur on different threads and therefore share one explicit mutex; rendering may never observe a torn multi-cell Blueprint.
- Selection, world-copy, thumbnails, and persistence are not implied by slot publication and remain active until independently implemented and accepted.

### Source, native package, and Windows runtime evidence

- `blueprint.hpp` owns bounded selection geometry, exact named cell payloads, lossless transform mapping, centered footprint validation, and transactional placement. `blueprint_contract.cpp` proves metadata preservation, rotation, transparency, invalid-material rejection, boundary rejection with no partial writes, trimming, and tile alignment.
- `SharedState` owns four synchronized Blueprint slots and explicit selected/placement/request state. Designer publication writes a slot without touching the world; Inventory selection activates only occupied slots.
- The render push reports honest occupancy, selection, kind, and transformed dimensions. Inventory and Designer draw real selected/occupied/empty slots, and Editor draws a valid/invalid camera-correct footprint instead of the normal brush while placement is active.
- The renderer canonicalizes Static Model cells at destination indices, retains exact Map Chunk cells, validates the complete transaction before upload, writes both resident buffers, and consumes/clears paste independently of pause while rejecting reset/load overlap.
- Native Windows and Linux Release builds compile all 12 shaders and C++ sources and pass 26/26 tests on each platform. Fresh installs and archive audits contain the executable, 12 SPIR-V shaders, installed Blueprint header, library/development files, launchers, and canonical documents. Published workflow SHA-256: Windows `8d2311ff24de30ee6ff245c9359dcc2ff7881326894c89fbb0eda31c151b8154`; Linux `60585da68cfb634767745d4ba1865f24f3ec424daa5bc97289ed3b210e5d565f`. Packaged Windows automation publishes Designer slot 1, enters PAUSED, completes a transactional world paste, renders Inventory Blueprint state beside the world, and exits cleanly. Broader mission acceptance remains active.
## v2.5.12 map overlay, blueprints, and lighting

| ID | Status | Mission | Acceptance |
|---|---|---|---|
| MC-149 | OPEN | Reusable bounded selection marquee | One camera-correct rectangular selection model supports cell and aligned-tile bounds, visible handles, cancellation, clipping, copy, cut, delete, Fill clipping, and map-chunk capture. Selection never mutates from keyboard input alone and never writes outside world bounds. v2.5.16 core evidence adds bounded inclusive geometry, point-order normalization, 8x8 alignment, strict world clipping, exact `SceneCell` capture, and deterministic contracts. The live marquee, handles, copy/cut/delete routing, and clipped Fill remain open. |
| MC-150 | REGRESSION | Sidebar Designer grid, blueprint library, preview, and paste | Designer is dedicated to multi-material placeable static models and exact copied map chunks, entirely inside the normal sidebar body. Its nested `INVENTORY` and `BLUEPRINTS` tabs, isolated material grid, independent palette/cursor/cells-tiles/shape/size/zoom state, and Static Model/Map Chunk modes never cover or replace the main viewport and never mutate world cells. Completion requires named slots, exact world-selection capture, thumbnails, rotation/mirroring where lossless, transactional preview/paste, save/load, practical sidebar canvas navigation, and direct publication into Inventory blueprint slots. v2.5.16 source/runtime evidence: four shared named slots preserve exact `SceneCell` payloads, deterministic rotation/mirroring mapping, transparent Static Model placement, full-state Map Chunk placement, all-or-nothing bounds/material validation, Designer direct publication, honest occupied/empty slot rendering, and a camera-correct footprint preview. Packaged Windows automation publishes slot 1, pastes it transactionally while PAUSED, and renders real Blueprint state beside the unchanged world; earlier source-run frames cover both Inventory and Designer Blueprint tabs. World-selection capture, thumbnails, persistence, and full canvas navigation remain active. |
| MC-151 | PARTIAL | Visible day/night cycle and sunlight-driven lighting | A deterministic six-minute simulation-time cycle begins at noon after reset, modulates ambient presentation, pauses with the simulation, reads the existing sunlight buffer for direct surface illumination, and preserves emissive Lava, Fire, Lightning, and Ember visibility through canonical material IDs only. Reset/load rebuilds sunlight before presentation. Every shader build must reject undefined or legacy material aliases, and every renderer binding must use the authoritative `sandhybrid::policy` namespace so native Windows/Linux compilation cannot diverge from headless contracts. Completion requires sun/moon presentation, configurable cycle controls, plant-growth coupling, artificial-light sources and occlusion, save/load time persistence, and packaged visual/performance acceptance. |
| MC-152 | REGRESSION | Simulation pause with live editing and complete deterministic reset | While paused, simulation, material animation, mineral glint, particles, actors, day/night, sunlight updates, and map snapshots remain frozen; camera/map/UI navigation and Editor mutations remain available. Paint, erase, Fill, Ignite Air, selection operations, and blueprint paste commit immediately and visibly against the frozen world without advancing physics/time or queueing a duplicate action for resume. Reset returns simulation and presentation time to zero, rebuilds both cell buffers, actor state, tile/chunk/debug metadata, sunlight, map snapshot, and camera/map framing without carrying queued one-shot input into the new scene. Packaged pause/edit/reset observation and repeated reset stress are required. v2.5.15 source evidence: direct paint, FILL, and IGNITE AIR remain renderer-authorized while paused. v2.5.16 fixes the player-scene/mining gate so paused direct editing remains available there, adds immediate transactional paused Blueprint paste, blocks paint/tool/deposit overlap on that click, and clears queued paste at load/reset boundaries. Windows Release automation visibly reaches PAUSED and logs a completed transactional paste; repeated packaged pause/reset stress and selection operations remain active. v2.5.17 focused evidence: Windows Release automation visibly pauses the app and commits Sand beneath the live cursor without resuming simulation; moving into the sidebar removes the preview while the committed edit remains. Erase/Fill/Ignite/selection coverage and repeated reset stress remain active. |

## Post-v2.5.16 packaged state-readback recovery

### Risk and impact review

- Static shader-token audits cannot accept macro-tile motion, fine fallback, Half Water motion/conservation, or hard-coded hive contents. The packaged executable needs a deterministic GPU seed/step/readback mode that exits nonzero and writes a machine-readable report when the observed cells contradict the mission.
- Runtime acceptance must be opt-in and must not change normal startup, input, saves, simulation cadence, presentation, or release behavior. It must use the same compiled pipelines and resident cell buffers as an ordinary packaged run.
- The diagnostic uses one canonical 640x360 authored envelope so native software Vulkan can finish deterministically without allocating a multi-region gameplay world. It dispatches only the production tile/macro/fine phases needed by each seeded check, bounds movement work to the first 192 columns and rows containing every movement scenario, and omits unrelated chemistry, actor, effect, and sunlight passes; full reset still covers the 640x360 hive. This changes only the opt-in acceptance configuration; every normal Compact/Standard/Large dimension and dispatch path remains unchanged.
- The v2.5.3 macro baseline requires both an intact 8x8 Water packet displacement and a blocked packet's fine-cell fallback; the test must distinguish exact packet transfer from ordinary cell motion and prove represented material is conserved.
- The accepted Half Water baseline creates two conserved halves only at a supplied full-Water ledge, then falls, attracts only across a clear two-to-four-cell gap, and merges adjacent compatible halves. The packaged check must exercise the production split branch directly so same-step merge passes cannot hide a creation or conservation failure.
- The Fix36 pre-PR19 Ecosystem hive's geometry and chamber contents are one hard-coded prefab. Reset, Beehive painting, and legacy-map normalization currently derive chamber entropy from different coordinates/salts, so matching radii alone is insufficient. One canonical 640x360 Ecosystem-local entropy function must reproduce the same Empty/Honey/Pollen pattern everywhere without importing SimpleSandSim bee behavior.
- Cursor acceptance must compare the logical native-window pointer, sidebar/world ownership, camera view, effective brush radius/shape, and committed cell. Framebuffer scaling may affect presentation only and must not be applied a second time to input.
- No affected mission becomes COMPLETE from this tranche alone. Native Windows and Linux Release builds/packages plus packaged runtime reports remain required before the next ordinary stable release.
### Source, native package, and publication evidence

- Task-chain recovery: the actual `SandHybrid v2.5.13 Update` task and its `v2.5.9 Release Update` predecessor were read. They preserve the 128-mission/five-pass handoff, cache-first impact review, sidebar Designer/Inventory intent, complete pause/reset intent, and the rule that runtime-only missions were not closed by source builds.
- Full Water root cause: CPU Fill/Blueprint/PPM construction used `0x00ffff00`, randomly setting the reserved `0x00800000` Half Water bit. CPU constructors now use `0x007fff00`; deterministic PPM coverage rejects a full Water cell carrying the half flag.
- Hive root cause: reset, Beehive painting, and loaded-map normalization matched the Fix36 radii but used different indexes/salts for chamber entropy. C++ and GLSL now share the canonical 640x360 Ecosystem-local `0xD17A5EED` entropy, with fixed Empty/Honey/Pollen examples and loaded-map assertions.
- Native Vulkan state-readback: the exact-source Windows Release executable on an NVIDIA RTX 5080 and Linux Release executable on Mesa llvmpipe run focused production pipeline dispatches and both report PASS for `macro_liquid_exact_packet`, `macro_gas_exact_packet`, `macro_blocked_fine_fallback`, `half_water_bounded_attraction_merge`, `half_water_falls_first`, `supplied_ledge_creates_half_water`, and `ecosystem_hard_coded_hive`. Observed values are Water 64/source 0/target 64; Hydrogen 64/source 0/target 64; blocked Water 64/source 56/moved 8; Half Water units 2 merged to one full cell; one half fell; ledge units 6/two half cells/four Water cells; hive mismatches 0/support 268/shell 209/Honey 18/Pollen 22/empty chamber 16.
- Cursor/pause evidence: C++ contracts map a 1920x1080 framebuffer to 1280x720 logical input. On the host's 150% display, direct Windows Release capture shows `PAUSED`, the preview centered on the committed Sand edit at logical (400,300), and no world preview at logical sidebar position (1000,300).
- No affected runtime mission becomes COMPLETE from this focused native evidence. Windows and Linux exact-source Release builds each pass 27/27 tests; both fresh installed packages pass the same seven Vulkan state checks with identical counts. Fresh Windows/Linux archives pass executable, 12-shader, 20-header, development-package, and 14-document audits. v2.5.17 is published as latest, non-draft, and non-prerelease after successful main/tag workflows. Published Windows SHA-256: `1aa90d0cbafc0b2703ebff3a8ecca9e5c30005578f663a3b5a77f4944735be44`; Linux SHA-256: `edc3690b08ea1c65bdb2ff2f79ce3341f3adf8517a8c4eb1617987d156d574bd`. All broader per-row scenarios remain active.
# Permanent invariants

- Every repository change begins with a mission-cache impact entry and explicit acceptance/risk review; implementation and release evidence follow that cached intent, never precede it.
- Every generated scene and loaded saved counterpart uses the same crystal-marker authored footprint at world Y=720, two complete sky rows above it, and stone-wrapped lower geology/lava stack below it.
- Scene PPM colors are unique, stable, paint-friendly representatives of visible cell colors and share one Save/Load/material-key table.
- Material save ID 31 is Beehive and ID 48 is Iron Ore; neither has a deprecated source alias.
- Material cells, actors, Atmosphere composition, and medium impulses are authoritative; hierarchy metadata only accelerates them.
- Behavior is canonical and provenance-independent.
- Gas/liquid volume, every gas component, pressure, and cross-boundary transfer are conserved.
- Normal air is one Atmosphere mixture; user-facing controls call it Air while the inspected material card retains the canonical Atmosphere identity.
- Excess-gas transit remains visible until accepted.
- Every gas and liquid can rest and wake deterministically.
- Damping never suppresses a valid gravity, buoyancy, pressure, erosion, reaction, tool, or actor-driven move. It only removes non-productive residual oscillation after local equilibrium.
- Gas-solid contact is collision only: solids prevent penetration but never apply tangential friction, sticky-wall drag, or adjacency-based sleeping to gas.
- Mud and wet bulk elements continue gravity-driven erosion at the same material-defined rate as equivalent fine cells.
- Mineral deposits are clustered into deterministic contiguous structural tile veins with rough edges; loose resource cells exist only as flagged sand-trap collapse material above a cavity, never as decorative confetti.
- Debug and map overlays must preserve the visible identity of Atmosphere, Water, and Half Water; hierarchy state uses restrained edges/markers rather than replacing whole medium fields with opaque status colors.
- Half Water never enters macro transfer. It falls first, consolidates with an adjacent half, may make only the early bounded two-to-four-cell attraction move through a clear gap, cannot use generic full-Water diagonal wandering, and cannot sleep while fall, merge, attraction, reaction, heat, or motion work is pending.
- Player/actor disturbance transfers bounded momentum/pressure without erasing or minting medium.
- Only the contiguous clipped/shifted 4x4 active window of whole 640x360 map-area regions animates; every other region is paused.
- The main thread never executes section simulation work.
- Worker assignment is deterministic and pairs outer map-area regions first when fewer than 17 workers are available.
- World expansion, zoom, pausing, streaming, and concurrency do not change deterministic reference results.
- Directional keyboard input has exactly one owner: character scenes default to the player, `WASD PAN` routes it to the simulation camera, and non-player scenes route it to the camera. MAP is a non-modal overlay; it receives only pointer-scoped wheel/right-drag interaction inside its own rectangle and never steals player/WASD ownership. Right-click exclusively owns captured drag and edge panning, middle mouse is unused, and right-click never edits world material.
- Debug statistics never cover the normal simulation viewport; debug state coloring and active-area boundaries may render in the world.
- 8x8 terrain regions qualify for stability only and never reconstruct missing cells.
- Each terrain pixel takes two laser hits; after more than half are dislodged, the represented remainder collapses rather than vanishes.
- Authored structural assemblies do not infer failure solely from missing direct-below support; they release only through explicit damage, phase change, mining, or the represented collapse threshold.
- Uniform atmosphere may sleep at sealed resident-world edges; world boundaries never manufacture perpetual gas activity.
- Industrial machines are active simulation participants, and every consumed represented input has a matching inventory/output transition.
- Project-owned branding is `SandHybrid`; Legacy branding remains only when it is part of a proper external dependency or integration name.
- Failed, missed, deferred, and regressed missions remain active until accepted.
- PAUSED freezes simulation and simulation-derived clocks, not editing. Authorized Editor mutations commit once against the frozen world and are immediately visible without advancing physics.
- Inventory and Designer are sidebar-only workspaces; each exposes nested INVENTORY and BLUEPRINTS tabs and never opens a center, floating, or modal body. Editor owns world cursor controls; Designer owns isolated cursor controls. The visible world cursor must use the exact position, shape, and radius of the committed edit and must disappear rather than clamp to a viewport edge while the pointer is in the sidebar.
- User-visible releases never use a prerelease marker or `-test` suffix.



# Accepted foundations

| ID | Accepted foundation | Evidence location |
|---|---|---|
| MC-153 | Latest visible stable release publication uses an ordinary monotonically increasing tag, no prerelease marker or `-test` suffix, matching source commit, two native packages, and two checksum assets. | `v2.5.17`; commit `791bf7160ce95ec27c5a31717e845cb6ba95e381`; main workflow `31069315698` and tag workflow `31069325459` succeeded; GitHub `isLatest=true`, `isDraft=false`, `isPrerelease=false`; published Windows SHA-256 `1aa90d0cbafc0b2703ebff3a8ecca9e5c30005578f663a3b5a77f4944735be44`; Linux SHA-256 `edc3690b08ea1c65bdb2ff2f79ce3341f3adf8517a8c4eb1617987d156d574bd`. |
| MC-070 | Reusable platform-neutral `SandHybrid::SandHybrid` core library, optional Vulkan runtime, thin demo host, installed CMake package, and downstream consumer contract. | `CHANGELOG.md`; Windows/Linux CI history. |
| MC-065 | Deterministic simulation code has no coroutine execution path. CI rejects coroutine machinery across production C++ until bounded streaming or I/O ownership and cancellation are defined under MC-063. | `tools/validate_no_simulation_coroutines.py`; `.github/workflows/core-hygiene-contracts.yml`; CI history. |
| MC-083 | `SandHybrid` is the only project-owned product identity across production code, build files, workflows, docs, and tests. Proper external names and the current repository-host URL remain explicitly allowed. | `tools/validate_project_branding.py`; `HALF_WATER.md`; `.github/workflows/core-hygiene-contracts.yml`; CI history. |
| MC-117 | Protected handoff `agent/nonoverlap-core-hygiene` at `bf2d052a9dbd275ce968cab9453b70b26b6a2771` was reconciled onto current `main` without dropping later mission-cache work. Adam explicitly required the source branch to remain preserved. | PR #45 validation history; PR #46 integration history; Windows/Linux v2.5.3 Release CI. |

Detailed release history belongs only in `CHANGELOG.md`. Completed source branches and one-shot patch machinery are deleted after publication.

### v2.5.7 runtime evidence

- `EV-257-001` — Water/Air: exposed media must remain fine-grained; no moving 8x8 checkerboard packets and no sleeping while a productive move exists. Links: MC-013, MC-017, MC-020, MC-124.
- `EV-257-002` — Lava: connected Lava remains Lava, slumps as a dense semi-solid, and is never floaty like Ash/Silt. Links: MC-125.
- `EV-257-003` — Vent: a Magma Vent produces into any non-solid outlet, pressurizes existing Lava, and stops only against solid blockage. Links: MC-125, MC-128.
- `EV-257-004` — Geology: metal deposits begin as full structural tiles; nearby Sand retains sparse loose resource cells. Links: MC-126.
- `EV-257-005` — Terrain generation: broken zones are minimized by construction; deliberate loose roofs are reserved for recognizable sand traps. Links: MC-126, MC-127.
- `EV-257-006` — Volcano screenshot: MAP and normal view must show a map-scale volcano and lake, not a tiny isolated apparatus. Links: MC-128.
