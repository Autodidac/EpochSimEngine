diff --git a/missioncache.md b/missioncache.md
index 8b56a5d..2ee84b6 100644
--- a/missioncache.md
+++ b/missioncache.md
@@ -298,7 +298,7 @@ This pass attempts the first 28 P0 IDs in listed order: `MC-012`, `MC-013`, `MC-
 |---|---|---|---|
 | MC-149 | OPEN | Reusable bounded selection marquee | One camera-correct rectangular selection model supports cell and aligned-tile bounds, visible handles, cancellation, clipping, copy, cut, delete, Fill clipping, and map-chunk capture. Selection never mutates from keyboard input alone and never writes outside world bounds. |
 | MC-150 | PARTIAL | Designer grid, blueprint library, preview, and paste | Designer is dedicated to multi-material placeable static models and exact copied map chunks. A 64x32 isolated material grid, independent palette/cursor/cells-tiles/shape/size/zoom state, Static Model/Map Chunk modes, and Inventory/Blueprints panes provide the first functional authoring surface without touching world cells. Completion requires named slots, exact world-selection capture, thumbnails, rotation/mirroring where lossless, transactional preview/paste, save/load, larger/resizable canvases, and direct publication into Inventory blueprint slots. |
-| MC-151 | PARTIAL | Visible day/night cycle and sunlight-driven lighting | A deterministic six-minute simulation-time cycle begins at noon after reset, modulates ambient presentation, pauses with the simulation, reads the existing sunlight buffer for direct surface illumination, and preserves emissive Lava, Fire, Lightning, and Ember visibility through canonical material IDs only. Reset/load rebuilds sunlight before presentation. Every shader build must reject undefined or legacy material aliases before packaging. Completion requires sun/moon presentation, configurable cycle controls, plant-growth coupling, artificial-light sources and occlusion, save/load time persistence, and packaged visual/performance acceptance. |
+| MC-151 | PARTIAL | Visible day/night cycle and sunlight-driven lighting | A deterministic six-minute simulation-time cycle begins at noon after reset, modulates ambient presentation, pauses with the simulation, reads the existing sunlight buffer for direct surface illumination, and preserves emissive Lava, Fire, Lightning, and Ember visibility through canonical material IDs only. Reset/load rebuilds sunlight before presentation. Every shader build must reject undefined or legacy material aliases, and every renderer binding must use the authoritative `sandhybrid::policy` namespace so native Windows/Linux compilation cannot diverge from headless contracts. Completion requires sun/moon presentation, configurable cycle controls, plant-growth coupling, artificial-light sources and occlusion, save/load time persistence, and packaged visual/performance acceptance. |
 | MC-152 | PARTIAL | Hard global pause and complete deterministic reset | While paused, simulation, material animation, mineral glint, particles, actors, tools, painting, Fill, Ignite Air, day/night, sunlight updates, and map snapshots remain frozen; camera/map/UI navigation remains available. Reset returns simulation and presentation time to zero, rebuilds both cell buffers, actor state, tile/chunk/debug metadata, sunlight, map snapshot, and camera/map framing without carrying queued one-shot input into the new scene. Static contracts pass; packaged pause/reset observation and repeated reset stress remain required. |
 
 # Permanent invariants
diff --git a/src/vulkan_renderer.cpp b/src/vulkan_renderer.cpp
index b652a65..9f52e90 100644
--- a/src/vulkan_renderer.cpp
+++ b/src/vulkan_renderer.cpp
@@ -2480,7 +2480,7 @@ const auto storage_usage = VK_BUFFER_USAGE_STORAGE_BUFFER_BIT | VK_BUFFER_USAGE_
             // every material effect and reset returns every effect to frame zero.
             .render_frame = simulation_step,
             .world_time = simulation_step,
-            .day_cycle_steps = sandhybrid::day_cycle_steps,
+            .day_cycle_steps = sandhybrid::policy::day_cycle_steps,
             .designer_flags = designer_flags,
         };
         vkCmdPushConstants(command_buffer, graphics_pipeline_layout, VK_SHADER_STAGE_FRAGMENT_BIT,
diff --git a/tools/validate_v2513_contract.py b/tools/validate_v2513_contract.py
index 9575f7b..8e04db1 100755
--- a/tools/validate_v2513_contract.py
+++ b/tools/validate_v2513_contract.py
@@ -126,5 +126,7 @@ for shader in sorted((ROOT / "shaders").iterdir()):
         raise SystemExit(f"{shader.relative_to(ROOT)}: undefined material IDs: {undefined}")
 require("shaders/fullscreen.frag",
         "cell.material == MAT_LIGHTNING || cell.material == MAT_EMBER")
+require("src/vulkan_renderer.cpp",
+        ".day_cycle_steps = sandhybrid::policy::day_cycle_steps,")
 
 print("v2.5.13 first-28 P0 tranche contracts valid.")
